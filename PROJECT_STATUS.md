# Stable 项目状态与开发交接

> 最后更新：2026-09-01
> 文档版本：`2026-09-01.5`
> 项目目录：`D:\Codex\2026-08-19\Stable-0.9.1`  
> 当前发布基线：`v0.9.34`
> 当前开发版本：`v0.9.39`
> 当前状态：**v0.9.39 已在未发布的 v0.9.38 全部能力上完成 Stable Cloud 桌面账号接入：安装版登录门禁、临时密码强制改密、Windows 加密设备会话、账号/金额额度/近 30 天用量、云端只读模型目录，以及为每次 Harness 模型请求生成独立幂等键的本机回环代理均已实现。141 项自动化测试、类型检查和生产构建通过；完整安装包与轻量更新包已在本机成功生成，打包后 `--stable-update-healthcheck` 退出码为 0。GitHub Hosted Windows 手动预检 Run `33431193467` 在提交 `5f20eb4` 上通过测试、两类打包、可见成功更新、可见失败回滚、静默失败回滚与证据上传，手动运行未发布 Release；预检分支随后已快进合入并推送远程 `main`，当前主分支为 `3012a56`。Stable Cloud Sites 已按用户明确授权改为公网可达，匿名 `/api/health` 返回应用 JSON 200，未登录 `/api/account` 返回应用 JSON 401，托管层不再阻断桌面客户端。发布前仅剩配置真实 DeepSeek 模型与服务端 Key、完成真实计费/超额拒绝验收，并创建 v0.9.39 标签与 Release。当前受管环境的 Chromium Renderer/GPU 子进程仍因缺失依赖退出，本轮 Electron 视觉截图未计为通过。**
> 适用读者：后续开发者、测试人员与项目维护者
> 文档范围：记录项目背景、目标、发布状态、本次会话、验证证据、未完成事项、卡点、踩坑与后续顺序
> 证据口径：`已发布` 表示远程 Release 已核验；`自动化验证` 表示测试或构建通过；`隔离验证` 表示本机 QA 环境通过；`待真机验收` 表示仍需用户设备给出最终结论

## 0. 当前开发版本 v0.9.39（发布准备中）

- 安装版默认启用 Stable Cloud 登录门禁；开发模式仅在显式设置 `STABLE_CLOUD_ENABLED=1` 时启用，避免既有本地开发与隔离测试被远程网络阻断。
- 登录请求携带持久设备 ID、设备名、平台和应用版本；账号密码只在本次请求中使用，成功后仅把 Bearer 设备会话写入 Electron `safeStorage`。
- 临时密码账号在进入工作台前必须修改密码；桌面端校验至少 12 个字符和两次一致，服务端改密会轮换令牌并撤销旧会话。
- 设置页新增账号身份、本期额度、剩余金额、额度周期和近 30 天请求/Token/确认消耗；刷新和退出均走主进程 IPC。
- 安装版使用 Stable Cloud 的 `/v1/models` 只读目录；模型、价格和供应商 Key 由管理员统一维护，对话输入区继续支持按对话选择模型。
- Harness 子进程只持有随机的本机回环代理凭据；真实设备令牌留在 Electron 主进程。代理只接受 `127.0.0.1`、随机进程凭据和固定 `/v1` 路径，并为每个 `/v1/chat/completions` 请求生成唯一 `Idempotency-Key`。
- 新增 8 项账号/网关/桌面集成测试；当前完整结果为 `npm test` 141/141、`npm run typecheck`、`npm run build` 通过。
- 本机 `npm run dist` 与 `npm run dist:update` 均成功；完整安装包为 `283855390` bytes，轻量更新包为 `115329902` bytes，打包目录中的应用健康检查退出码为 `0`。
- 预检分支 `codex/v0.9.39-preflight` 已推送；GitHub Actions Run `33431193467` 成功，成功升级终态为 `100|complete|success|0`，没有自动重启且用户数据保留；可见和静默强制失败均回滚到 v0.9.31，终态为 `92|healthcheck_rollback|failed_rolled_back|12`。
- 预检通过后已将 `codex/v0.9.39-preflight` 快进合入并推送远程 `main`；当前主分支提交为 `3012a56`，仍未创建 v0.9.39 标签或 Release。
- Stable Cloud Sites 已从仅所有者访问切换为公网可达；匿名 `/api/health` 返回 JSON 200，未登录 `/api/account` 返回 Stable Cloud 自身 JSON 401，首页返回 200，业务数据继续受应用账号鉴权保护。
- Electron 登录页与登录后设置页截图尝试因当前环境已有的 Chromium Renderer/GPU 子进程缺失错误失败，未计入视觉验收。
- 已完成本地打包、GitHub Hosted Windows 安装器隔离 QA、主分支合入和云端公网门禁；仍未创建标签/Release、未配置真实供应商 Key、未执行真实模型调用或其他设备更新验收。

## 0.1 上一开发版本 v0.9.38（未发布）

- Agent 回答中的 Stable 工作区绝对路径会生成原生按钮文件卡片；已发送并复制到工作区的上传附件使用同一套卡片，待物化附件和普通资源引用仍保持不可点击。
- 点击文件卡片或对话中的本地文件链接会打开当前窗口右侧的可调宽预览；切换、新建或删除对话会关闭旧预览，快速连续打开只允许最新请求更新侧栏；原“本次上下文”顶部按钮、右侧面板、响应式布局和 QA 查询均已删除。
- 本地预览、系统打开、“打开所在文件夹”和附件暂存统一执行工作区边界校验；UNC 文件 URL、缺失、外部、同名前缀兄弟目录及工作区内 Junction/符号链接逃逸会在探测或复制前拒绝，随后再执行 `realpath` 二次校验。
- Markdown、文本、结构化文本、Office/PDF 提取内容、图片、静态 HTML 和未知格式文件信息均有安全侧栏路径；本地 partition 禁用 JavaScript、联网、权限、下载、新窗口、WebView 和拖放导航。
- 通用 Agent 提示词明确要求任何格式的生成或修改文件真实写入当前 Stable 工作区、完成检查，并把每个已验证绝对路径各放一行供界面生成文件卡片；已分类交付任务另有更严格的完成检查。
- 临时附件不再按提取正文总字符数拒绝；通过最多 8 个附件、合计 100 MB、文件夹或 ZIP 最多 2000 个文件及既有路径安全检查后，普通附件原件完整复制到工作区。
- 提示词附件正文改为显式标注的头尾有界预览：全部附件共享 24000 字符预算、单项最多 6000 字符；Agent 被明确要求通过完整路径搜索并按最多 16000 字符分段读取省略内容。
- 文件夹与 ZIP 的 300000 字符提取上限现已包含文件标题前缀；预处理硬失败会清理本批未提交副本。
- 发送消息尚未被主进程接受时，输入区会合并恢复原草稿、附件和引用；错误在输入区内联展示，全局错误提示移到顶部，不再遮挡权限、模型、能力和发送按钮。
- “设置 → 模型服务”可保存多个独立模型配置；每项具有稳定 ID、服务 ID、API 地址、模型名和独立加密 API Key，只向渲染进程返回 `hasApiKey` 状态。
- 新对话会快照创建时的全局默认模型；修改默认模型不改变已有对话。输入区可为当前对话切换模型，并明确提示“从下一条消息生效”。
- 每个模型任务在开始时冻结模型配置和 API Key；运行中编辑配置或切换对话模型，不会改变本次主 Agent、审批 Agent、续跑或自动化意图识别所用的路由。
- 旧版单模型设置、旧加密 `apiKey` 和既有对话会幂等迁移；旧设置与旧密钥暂时保留，兼顾回退读取。
- 每次 Harness 运行使用独立 `DSH_HOME/settings.yaml`，不同对话并发使用不同模型时不会互相覆盖。非 DeepSeek 模型的 Key 不再冒充 `DEEPSEEK_API_KEY`。
- Harness Home 清理改为逐项 `lstat`：遇到 Junction/符号链接只删除链接本身，不进入链接目标；同时把目标父目录严格绑定到本次 Runner 的确切 `userData/harness/runs`，只接受其直接子目录 `run-*`。
- 验证：`npm test` 133/133、`npm run typecheck`、`npm run build`、关键 CommonJS 语法检查、Electron Junction 回归和五次 Electron-hosted 本机模拟 Harness 集成均通过；深色/浅色 1440×900 隔离 Electron 中生成卡片、上传卡片触发、50% 预览栏、加载完成状态和无页面横向溢出通过；生产构建仍有既有的单个 JavaScript chunk 大于 500 kB 警告。
- 本轮未运行安装包构建、GitHub Actions、真实服务商 API 调用或真实用户文件桌面人工交互验收；未创建提交、标签或 GitHub Release。

## 0.2 当前正式版本 v0.9.34

- 更新安装器复用 NSIS 原生安装页进度条和状态文本，按 `1% → 4% → 10% → 55% → 62% → 68% → 70% → 76% → 82% → 86% → 88% → 90%/94% → 95% → 100%` 报告阶段进度。
- `100%` 只在新版健康检查、安装记录、卸载信息和快捷方式全部完成后出现；成功页短暂停留后安装器才关闭。
- Electron 使用 `quitAndInstall(false, false)`：安装完成后不自动重新启动 Stable，用户重新点击桌面或开始菜单图标打开 `v0.9.34`。
- 更新过程中关闭按钮受保护；若健康检查失败，安装器恢复旧程序和旧 Runtime，并保持失败结果页打开，显示“旧版本已恢复”和错误码。
- 轻量更新继续复用旧安装的 Runtime；完整安装包继续包含 Runtime，仅用于首次安装或修复缺失 Runtime。
- GitHub Hosted Windows 已分别验证可见成功更新、可见失败回滚和静默失败：成功终态 `100|complete|success|0`；回滚终态 `92|healthcheck_rollback|failed_rolled_back|12`。
- 正式发布提交：`868834327f0679bbbe9ced3686299d8a4f574291`；标签：`v0.9.34`。
- 预发布门禁运行：`33312878503`，耗时 `21m21s`，发布步骤按设计跳过。
- 正式标签工作流：`33313929332`，耗时 `19m4s`，测试、构建、安装器 QA、证据上传和 Release 资产发布全部通过。
- `v0.9.32`、`v0.9.33` 标签工作流曾失败，均未创建公开 Release；当前线上最新正式版本直接从 `v0.9.31` 更新为 `v0.9.34`。
- GitHub Release 四项资产、匿名下载 HTTP 200、GitHub 远端 SHA-256 摘要和远程 `latest.yml` 已完成发布后复核。
- 上述结果是 GitHub 托管隔离机验证，不等于用户另一台真实旧版本设备的网络、权限、杀毒软件、快捷方式和数据保留闭环；真机验收仍是下一步。

## 0.3 上一正式版本 v0.9.31

- 远程更新改用不包含 Harness Runtime 的 `Stable-Update-0.9.31-x64.exe`；完整 `Stable-Setup-0.9.31-x64.exe` 只用于首次安装。
- 旧安装中的 Runtime 在首次升级时同盘迁移到 `%LOCALAPPDATA%\stable-desktop\runtime-v1`，以后版本直接复用，不再重复解压和复制约 3.8 万个文件。
- 新版本先完整解压到安装目录旁的暂存目录，旧程序保持可用；暂存完成后才通过目录重命名快速切换。
- 切换后执行 `--stable-update-healthcheck`；失败时关闭残留 Electron 子进程、恢复旧目录和旧 Runtime，恢复失败时不启动新程序。
- 重新启用 electron-updater blockmap 差分下载；下载失败仍可回退到完整轻量更新包。
- 按固定规则，版本号已从 `v0.9.30` 递增到 `v0.9.31`。
- 隔离测试结果：成功更新约 `6.98` 秒，健康检查退出码 `0`；强制失败回滚约 `8.05` 秒，安装器退出码 `12`，旧 EXE 与 Runtime 完整恢复。
- 完整首次安装仍需展开全部 Runtime，本机实测约 `1020.14` 秒；该慢路径已从远程更新中移除。
- 本地完整安装包：`release-0.9.31/Stable-Setup-0.9.31-x64.exe`，`283844927` 字节，SHA-256 `2AF4588409F51F7D207624E6877AA0D23836006D5544D652E7612CCEDD3AD218`。
- 本地轻量更新包：`release-0.9.31-update/Stable-Update-0.9.31-x64.exe`，`115319891` 字节，SHA-256 `9D0169F6789BB4F1D47CAA9644338C88064075680D35B8AF26009DB493215DB9`。

## 0.4 更早正式版本 v0.9.30

- 包含 v0.9.29 的任务操作区、输入框交互、对话文字和普通 ZIP/文件夹附件界面改进。
- 用户主动上传的普通文件、ZIP 和文件夹会自动复制到 `workspace/.stable/attachments/<conversation>/...` 隔离目录；Agent 获得该副本的真实路径，可直接解压、安装或运行。
- 附件权限只覆盖本次主动选择并复制的副本，不开放原始文件所在目录或其他本机路径。
- NSIS 改用 7z 临时解包和复制重试，不再使用直接写入安装目录的 ZIP 插件；恢复安装包 blockmap 与差分更新资产。
- 按固定规则，版本号已从 `v0.9.29` 递增到 `v0.9.30`。
- 验证结果：108 项 Node 自动化测试、TypeScript 类型检查、Vite 生产构建、NSIS 打包、隔离全新安装、安装后离屏启动和测试卸载全部通过。
- 本地验证安装包：`release-0.9.30/Stable-Setup-0.9.30-x64.exe`，大小 `283894295` 字节，SHA-256 `A8033D12299DDE257F09A8E6A49E7AC0ED12198A24E21EFFC901DB8626956365`。

## 1. 一句话结论

Stable 已从早期桌面 Agent 原型发展为 Windows 本地优先、由 Stable Cloud 统一管理内部账号、额度和模型的 Agent 工作台。当前开发版本 `v0.9.39` 已完成安装版登录门禁、强制改密、加密设备会话、金额额度与使用统计、云端模型目录和逐请求幂等网关，并继承 v0.9.38 的文件卡片、多模型、大附件和 Runtime 安全修复；141 项测试、类型检查、生产构建、本地打包和 GitHub Hosted Windows 安装器预检均通过。Stable Cloud 已公网可达且未登录业务接口仍返回应用 JSON 401。当前正式版本仍为 `v0.9.34`；发布前必须配置真实供应商模型/密钥并完成真实计费、超额拒绝和其他设备更新验收。

更新发布状态：2026-08-27 `github.com/LLucas88/Stable` 已改为公开仓库，匿名访问仓库及最新 Release API 均返回 200。客户端不内置 GitHub 访问令牌。`runtime-v1` 预发布已公开上传并通过 SHA-256 与必需入口检查，GitHub Actions 会复用其中的 `stable-runtime-win-x64.zip`。

## 2. 项目背景

Stable 是一个 Windows 本地优先的 Agent 桌面工作台，能够在本机组织数据、脚本、知识库、Skills 和工作流。项目从早期对话原型逐步扩展到资源管理、真实文件交付、模块化工作流、定时自动化、Team 对话快照和应用内更新。

- 在本地保存并切换多个 Agent 对话任务。
- 只在需要时向 Agent 提供数据、脚本、知识库、Skill 或临时附件。
- 将 Agent 能力拆成可连接、可执行、可观察的工作流模块。
- 支持半自动、需要控制台输入的脚本包。
- 将 Markdown、HTML、Excel、PowerPoint 等真实文件作为工作流交付物。
- 让同一局域网内的 Stable 设备发送和接收“发送时刻的对话快照”，而不是持续同步全部对话。
- 在执行过程中展示工具、AI 处理和子 Agent 的运行状态，同时保留用户可控的权限边界。

## 3. 核心目标

### 3.1 产品目标

1. **Local First**：数据处理、脚本执行、资源管理和大部分 Agent 调用尽量在本机工作区内完成。
2. **模块化**：数据、知识、脚本、Skill、AI 运算和输出均能作为独立模块组织与复用。
3. **可观察**：用户能够知道 Agent 调用了什么资源、执行到哪一步、是否产生真实交付物。
4. **可控权限**：工作区内、需要审批和完全访问等权限模式有清晰边界。
5. **按需上下文**：默认不把整个工作区、全部数据库或全部知识库发送给模型，只加载用户手动引用或检索命中的内容。
6. **跨端协作**：Team 功能以可确认、可拒绝的对话快照为主要协作对象。
7. **可交付**：对话和工作流不仅输出文字，也能产生并打开真实文件。
8. **可维护发布**：修复或新增需求完成后同步递增版本号；正式 Release 同时提供完整首次安装包、轻量更新包、blockmap 和正确的更新清单。

### 3.2 当前版本不追求的内容

- Team 对话的实时双向持续同步。
- 多个独立工作流同时运行；当前并发重点是同一工作流中互不依赖的分支。
- 在 Team 页面继续保留旧的“派发远程任务”和“任务与审批”前端模块。
- 默认将完整工作区清单发送给模型。

## 4. 技术基线

| 层级 | 当前技术/位置 | 说明 |
| --- | --- | --- |
| 桌面壳 | Electron `43.4.0` | 主进程：`desktop/main.cjs`；预加载：`desktop/preload.cjs` |
| 前端 | React `18.3.1` + TypeScript `5.7.3` + Vite `6.1.0` | 主要入口：`src/App.tsx`、`src/main.tsx` |
| 工作流画布 | `@xyflow/react` | 主要实现：`src/WorkflowStudio.tsx` |
| 本地 Agent | DeepSeek Harness 本地运行时 | 运行时位于本地 `runtime/`，不提交到 Git |
| 本地存储 | Electron 主进程服务 + Node SQLite | 资源、对话、工作流和运行状态由 `desktop/services/store.cjs` 持久化 |
| 资源处理 | JSZip、XLSX、Mammoth、pdf-parse、PptxGenJS | 支持文件夹/ZIP、Office、知识和输出文件处理 |
| Team 通信 | WebSocket / 本地 Team relay | 对话快照、设备发现与接收确认 |
| 打包 | electron-builder + NSIS x64 | `npm run dist`；安装包包含本地运行时 |

## 5. 当前已具备的能力

下表中的“已验证”表示当前源码测试覆盖且本轮检查通过；“源码存在”表示在源码中可见，但本轮没有完成真实 GUI 或跨设备端到端验证。

| 模块 | 当前状态 | 说明 |
| --- | --- | --- |
| 多任务 Agent 对话 | 已验证 | 对话独立保存、切换和运行，不因切换 Tab 丢失 |
| 多模型目录与对话级切换 | v0.9.35 源码与测试通过，未发布 | 模型配置拥有独立服务地址和加密 Key；新对话继承创建时默认模型，已有对话可从输入区切换 |
| 对话资源引用 | 已验证 | 数据、脚本、知识库、Skill 和临时附件可按需加入当前请求 |
| 大附件按路径读取 | v0.9.36 源码与测试通过，未发布 | 原件完整复制；提示词只放共享预算的有界预览，失败时恢复未提交草稿与资源 |
| 默认上下文控制 | 已验证 | 不默认发送整个工作区清单；仅手动引用或明确检索时加载 |
| Skill 安装与预览 | 已验证 | 可从对话安装完整 Skill 文件夹；Skill 库预览原始 `SKILL.md` |
| 脚本包导入 | 已验证 | 支持单文件、完整文件夹和 ZIP，保留入口脚本及同级依赖 |
| 交互式脚本 | 已验证 | 支持多轮 stdin、CMD/PowerShell/Node 包装链及控制台问题识别 |
| 模块化工作流 | 已验证 | 可添加、删除、连接节点，支持并行分支、汇合和边删除 |
| 工作流输出 | 已验证 | 可创建 Markdown、HTML、Excel 和 PowerPoint 文件 |
| 结果出口节点 | 已验证 | 工作流完成后出现临时出口节点，可打开文件或所在文件夹 |
| Team 对话快照 | 已验证 | 在线设备选择、发送、接收端接受/拒绝、接受后继续对话 |
| 权限模式 | 已验证 | 对话保留请求审批、Agent 辅助审批、完全访问等显式模式 |
| 执行过程卡片 | 已验证 | 工具、上下文和 AI 处理按模块归类展示 |
| 子 Agent 运行卡片 | 已发布，测试通过 | 显示子 Agent 名称、状态、当前任务和最新动作 |
| Team 收起/展开 | 已发布，测试通过 | 收起后只保留 Team 图标 |
| 对话记录卡片样式 | 已发布，测试通过 | 本机任务记录位于常驻卡片区域 |
| 执行计时器 | 已发布，测试通过 | 从发送开始计时，完成、失败或停止后固定最终时长 |
| 复制用户消息 | 源码与界面验证通过 | 图标位于消息气泡右下方；覆盖主输入框并恢复资源；`Ctrl+Z` 可撤回到原草稿 |
| 用户消息即时展示 | 已发布，测试通过 | 消息写入数据库后立即进入对话，不等待模型任务完成；复制后发送同样适用 |
| 默认 Coding 能力 | 已发布，测试通过 | 新对话默认开放工作区读写、命令、依赖安装、联网工具和最多三层子 Agent；破坏性操作仍需审批 |
| 网页/Markdown 对话侧栏预览 | 源码、安全与界面验证通过 | 仅由对话中的链接或 Markdown 文件触发；默认占工作区 50%，可拖动或键盘调宽 |
| 联网搜索凭据 | v0.9.35 源码与测试通过 | 当前模型 Key 只注入短生命周期子进程的 `STABLE_API_KEY`；仅 DeepSeek 模型会同步映射到 `DEEPSEEK_API_KEY`，非 DeepSeek 模型不会冒充搜索凭据；尚待真实查询 |
| Markdown 表格复制 | 源码与测试通过 | 表格无悬停位移动画；悬停或键盘聚焦时显示右上角图标，只复制 Markdown 表格 |
| 全局 `AGENTS.md` 设置 | 已发布，测试通过 | 读写应用数据目录中的文件，只注入之后启动的新任务 |
| 定时自动化 | 已发布，测试通过 | 支持一次、每天、每周和每月；仅在 Stable 开启时调度；支持定时页手动创建和对话确认创建 |
| 应用内远程更新 | v0.9.34 已发布，托管端到端验证通过 | 下载后打开带百分比的安装窗口；100% 完成后才关窗，不自动重启；失败保持回滚结果页；真实旧设备升级尚待完成 |

## 6. 本次会话做了什么

### 6.1 复现并定位远程更新故障

1. v0.9.27 的更新虽然能够下载，但用户仍需手动进入安装程序，安装过程也明显偏慢。
2. v0.9.30 在另一台设备执行远程更新时，Stable 退出后长时间无法重新打开，桌面快捷方式失效，安装目录一度不存在；设备没有出现新的 UAC 提示，应用经过较长时间后才可能恢复。
3. 代码、安装包结构和隔离安装结果表明，远程升级仍承受完整安装包的 Runtime 展开成本：完整包包含约 3.8 万个 Runtime 文件，本机完整首次安装实测约 `1020.14` 秒。旧程序在新版本完全准备好之前不可用，会把长时间解压表现成“软件消失”。
4. 本轮把“下载完成”与“升级可用”分开判断，没有再用构建通过代替真实设备升级结论。

### 6.2 完成 v0.9.31 更新架构

1. 按固定版本规则将应用从 `v0.9.30` 递增到 `v0.9.31`。
2. 将发行物拆成包含 Runtime 的完整首次安装包和不包含 Runtime 的轻量远程更新包；远程 `latest.yml` 只指向轻量包。
3. 将 Harness Runtime 迁移到 `%LOCALAPPDATA%\stable-desktop\runtime-v1`，后续更新复用该目录；应用优先使用持久 Runtime，并保留安装目录内 Runtime 作为旧版兼容回退。
4. 新安装内容先解压到当前安装目录旁的暂存目录，暂存完成后再执行目录切换，避免在长时间展开文件时提前破坏旧安装。
5. 切换后运行 `--stable-update-healthcheck`；健康检查失败时恢复旧程序与旧 Runtime，恢复不完整时不误启动新程序。
6. 恢复 electron-updater 的 blockmap 差分下载，同时保留完整轻量包回退。
7. 自定义 NSIS 安装与卸载脚本支持自定义安装目录、`_?=` 精确卸载路径、持久 Runtime 清理和异常回滚。

### 6.3 完成验证与正式发布

1. `npm test` 共 `110/110` 项通过，`npm run typecheck`、Node/CJS 语法检查和 `npm run build` 通过。
2. 隔离成功更新约 `6.98` 秒，安装和健康检查退出码均为 `0`；应用正常启动后才清理旧备份。
3. 强制健康检查失败时约 `8.05` 秒完成回滚，安装器退出码为 `12`，旧 EXE 与 Runtime 完整恢复。
4. 完整首次安装、安装后健康检查、自定义目录安装和 QA 卸载通过；完整安装仍是慢路径，但已从远程更新中移除。
5. 发布提交 `fd8c6bd` 与标签 `v0.9.31` 已推送；GitHub Actions 运行 `33298757511` 在 `7m11s` 内完成测试、构建与四项资产上传。
6. GitHub 最新正式 Release 已指向 `v0.9.31`；完整安装包、轻量更新包、轻量包 blockmap 和 `latest.yml` 的匿名直链均返回 HTTP 200。
7. 远程 `latest.yml` 已确认指向 `Stable-Update-0.9.31-x64.exe`，没有误指向完整安装包。

### 6.4 更新项目交接记录

1. 将当前发布基线统一更新为 `v0.9.31`，并记录本地与远程发行物的独立大小和哈希。
2. 将历史 v0.9.27 更新测试保留为背景证据，不再把它列为当前版本的首要验收目标。
3. 明确区分已发布、自动化验证、隔离验证和待真机验收，避免后续接手者把测试结果误写成用户设备结论。

### 6.5 完成 v0.9.34 可见更新安装与正式发布

1. 将更新安装从后台无感过程改为单一 NSIS 原生进度页，持续显示阶段百分比和中文状态，不创建额外不稳定的原生窗口。
2. 将成功关闭条件放到新版文件切换、健康检查、安装记录和快捷方式全部完成之后；`100%` 页面短暂停留后安装器关闭。
3. 将 Electron 安装调用改为 `quitAndInstall(false, false)`，取消安装完成后的自动启动；用户重新点击 Stable 图标即可打开新版。
4. 健康检查失败时保留失败页，显示回滚结果与错误码；静默模式仍以非零退出码结束，不被可见 UI 逻辑影响。
5. 修复跨卷 Runtime 回滚、进度日志持久追加、PowerShell 进程退出码读取和 GitHub Hosted Windows GUI QA 的确定性问题。
6. `v0.9.32`、`v0.9.33` 的失败标签没有发布 Release；从 `v0.9.34` 起先以 `workflow_dispatch` 运行不发布的完整预检，绿色后才推送正式标签。
7. 预检运行 `33312878503` 在提交 `8688343` 上完整通过；正式标签运行 `33313929332` 再次通过并发布四项资产。
8. 发布后复核最新 Release、四项远端 SHA-256、匿名 HTTP 200 和 `latest.yml`；从公开链接重新下载轻量更新包，SHA-256 与 GitHub 摘要、SHA-512 与 `latest.yml` 均一致。

### 6.6 完成 v0.9.35 多模型源码与测试

1. 将单一全局模型设置迁移为可扩展的模型目录；每个模型配置保留稳定内部 ID、服务 ID、显示名称、Base URL、真实模型名和独立加密凭据。
2. 给对话增加持久化 `model_id`：旧对话幂等绑定迁移模型，新对话快照当前默认模型，修改默认模型不追溯改变已有对话。
3. 设置页增加模型卡片、新增/编辑、DeepSeek 预设、设为默认和受保护删除；输入区增加原生 radio 模型选择器、键盘焦点和“从下一条消息生效”状态提示。
4. 主进程在消息开始、附件异步处理之前冻结模型与 Key；自动化意图识别、主 Agent、审批 Agent和文件交付续跑共用该快照。
5. 工作流、脚本自动回答、Team 规划与综合等旁路也改为通过统一模型路由解析，不再直接读取旧全局 `settings.model` 与 `secrets.apiKey`。
6. Harness 每次执行生成独立 `DSH_HOME/settings.yaml` 并在进程关闭后清理；并发模型配置不会覆盖，配置文件只保存环境变量引用、不保存明文 Key。
7. 版本递增到 `v0.9.35`，新增模型迁移、凭据隔离、运行快照、并发 Harness 与 UI 契约测试；`npm test` 125/125、类型检查和生产构建通过。
8. 严格遵守本轮边界：没有打包、没有运行发布工作流、没有创建标签或 Release，也没有把自动化测试写成真实服务商或 GUI 验收结论。

### 6.7 完成 v0.9.36 大附件发送修复

1. 确认截图错误发生在附件已复制到工作区之后、用户消息落库之前；根因是主进程把提取正文的 300000 字符预览上限误当成附件准入条件。
2. 删除正文总字符数硬拒绝，保留数量、总字节、文件数、符号链接和 ZIP 路径等物理安全边界；解析硬失败时清理本批已物化但未提交的目录。
3. 提示词层独立实施全部附件 24000 字符、单项 6000 字符预算，保留每个名称与完整路径，使用头尾节选、截断状态和分段读取约束，避免 8 个附件最坏注入 80 万字符。
4. 修复文件夹/ZIP 标题前缀未计入 300000 字符预算的问题；大文本原件物化测试验证尾部哨兵仍保留在工作区副本中。
5. Renderer 仅在主进程尚未接受消息时合并恢复草稿、附件和引用；发送错误在 Composer 内联显示，全局错误移到顶部。
6. 版本递增到 `v0.9.36`；`npm test` 129/129、类型检查、生产构建和关键 CommonJS 语法检查通过。未打包、未发布，未运行真实模型或 Electron GUI 人工验收。

### 6.8 完成 v0.9.37 Harness Junction 永久修复

1. 两次截图中的“Stable 的 Harness 运行时不完整，请重新安装”都发生在 `HarnessRunner.run()` 的运行时就绪检查阶段；模型配置和上下文准备尚未进入真实模型调用。第一次整体恢复后，一个成功任务结束又使下一次任务立即失败，证明不是一次性文件缺失。
2. 逐时序比对确认：每次成功 Agent 结束时，独立 `DSH_HOME/profiles/node_modules` 内约 509 个 Windows Junction 指向真实 Runtime 包目录；Electron 43.4.0 内置 Node v24.18.1 执行 `rmSync(dshHome, { recursive: true })` 时跟随 Junction，删空了所有目标目录，并连带删空项目 `node_modules` 中的 `csstype`、`@types/prop-types`、`@types/react`。
3. 普通 Node v24.14.0 对相同 Junction 探针不会删除目标，而 Electron-hosted Node 会删除目标，因此此前 `npm test` 和普通 `npm run test:integration` 都可能给出假阴性；Windows Defender 同期没有检测或隔离记录。
4. `desktop/services/harness.cjs` 新增不跟随链接的递归清理：先 `lstat`，Junction/符号链接只 `unlink`，普通目录才枚举子项；清理入口同时绑定本次 Runner 的确切 `userData/harness/runs`，只接受其直接 `run-*` 子目录，其他根下外形相同的路径也直接拒绝。
5. 从已验证的同版本来源再次整体恢复正式 `runtime/`，并机械恢复三个被连带删空、且版本与 `package-lock.json` 一致的项目依赖。正式 Runtime 为 37,847 个文件、730,332,578 bytes，关键 `dsh/lib/bin.js` SHA-256 为 `C0226687BB20F45C603EC6FE50F3DE16D1C3510C3A803304EC575EF9BC366C62`。
6. 两次损坏现场均保留在仓库外：`D:\Codex\2026-08-19\Stable-0.9.1-runtime-backups\runtime-broken-20260831-1029` 和 `D:\Codex\2026-08-19\Stable-0.9.1-runtime-backups\runtime-broken-20260831-1131`。
7. 新增实际调用项目 Electron 的 Junction 回归；`npm test` 130/130、类型检查、生产构建和 CommonJS 语法检查通过。完整本机模拟 Harness 在 Electron host 下连续执行五次均得到 `HARNESS_INTEGRATION_PASS requests=1 events=4`；执行后 Runtime 数量、字节、关键哈希与三个项目依赖文件数全部不变。源码版已重启为 `v0.9.37`，进程响应正常，`ready=true`，运行临时目录为空。
8. 本次没有修改用户数据、模型设置或 API Key；验证使用本机模拟 Chat Completions 服务，不消耗真实模型额度。当前仍未打包、未发布，也未把进程级和自动化结果写成人工界面验收。

### 6.9 完成 v0.9.38 文件卡片与工作区侧栏预览

1. 找到并核对历史 `v0.1.26` 的真实实现，只迁移“回复中的生成文件路径变成卡片、路径必须位于工作区且真实存在、提示词明确交付路径规则”这一组能力，没有直接覆盖当前版本已经演进的对话、模型、附件和安全逻辑。
2. Agent 回复里的工作区绝对路径现在显示为可点击的“生成文件”卡片；已物化到当前对话工作区的上传附件也显示为可点击的“上传附件”卡片。卡片使用原生按钮，支持键盘焦点、按压和悬停状态，并保留文件名、类型和大小信息。
3. 点击任一卡片都会在对话右侧打开统一文件预览栏；切换、新建或删除对话会关闭旧预览，快速连续打开时旧的慢请求不能覆盖新请求或污染新侧栏状态。原“本次上下文”按钮、统计面板、状态和相关布局逻辑已删除。
4. 预览支持 Markdown、纯文本、CSV、JSON、YAML、日志、XML、图片，以及可安全提取的 PDF、Word、Excel；HTML 只做禁用 JavaScript、禁止网络、禁止新窗口和禁止下载的静态预览，目录或暂不支持的二进制格式显示文件信息页。
5. 新增“lexical 边界预检 + `realpath(workspace) + realpath(target) + path.relative` 二次校验”守卫。预览、系统打开和“在文件夹中显示”都只使用守卫返回的规范路径；带主机名的 `file:` URL、缺失文件、工作区外文件、同名前缀兄弟目录、目录误用和工作区内 Junction/符号链接逃逸均会被拒绝。
6. 附件暂存逐级 `lstat` 拒绝 Junction/符号链接，复制前后复核 canonical 工作区边界，失败回滚使用不跟随链接的清理；上传附件不会因预置 Junction 被写到工作区外。
7. 通用 Agent 提示词要求任何格式的生成或修改文件保存到当前 Stable 工作区，交付前确认真实存在，并把每个绝对路径单独成行；已分类交付任务保留更严格检查，相应规则和路径安全场景均增加回归测试。
8. 开发版本递增到 `v0.9.38`；`npm test` 133/133、类型检查、生产构建和关键 CommonJS 语法检查通过。隔离 Electron 浅色/深色 QA 验证生成卡片、上传附件卡片、50% 右侧预览、无旧上下文 UI、无水平溢出和本地预览 JavaScript 关闭，Runtime `ready=true`。最终持久源码重启在当前受管环境中因 Chromium Renderer/GPU 子进程启动失败而未保持运行；本轮没有打包或发布。

### 6.10 完成 v0.9.39 Stable Cloud 桌面账号接入

1. 新增主进程账号客户端，支持设备登录、会话恢复、首次改密、账号/额度/用量刷新和退出；密码不落盘，设备 Bearer 令牌只进入 Windows `safeStorage`。
2. 新增安装版登录门禁和受控表单；允许密码管理器与粘贴，失败同时提供可聚焦错误摘要，临时密码账号不能跳过改密进入工作台。
3. 设置页显示账号身份、金额额度进度、剩余金额、周期截止日和近 30 天请求、Token、确认消耗；云端模型目录为只读，管理员统一维护价格和供应商密钥。
4. 新增只监听 `127.0.0.1` 的随机凭据代理。Harness 只持有本机一次性凭据，代理使用主进程设备会话转发，并为每次 Chat Completions 生成唯一幂等键。
5. 安装版强制使用云端模型；开发版需显式启用云端模式，保留既有本地模型测试与离线开发路径。
6. 新增账号、改密、恢复、代理鉴权、幂等键、IPC/UI 和云端模型路由回归；完整结果为 141/141、类型检查和生产构建通过。
7. 公网只读检查确认 GitHub v0.9.34 Release 与发布登录正常；Sites 访问模式随后按用户授权切换为 `public`，匿名健康检查返回应用 JSON 200，未登录账号接口返回应用 JSON 401。
8. 使用本机模拟 Cloud 尝试 Electron 登录页和账号页截图，但当前受管环境的 Renderer/GPU 子进程仍以缺失依赖退出，本轮视觉验收未计为通过。

## 7. 当前代码状态

当前分支为 `main`。`v0.9.34` 为正式发布基线，工作区开发版本为发布准备中的 `v0.9.39`：

| 文件 | 基线状态 | 当前内容 |
| --- | --- | --- |
| `src/App.tsx`、`src/WorkflowStudio.tsx` | v0.9.38 未发布源码 | 生成文件卡片、上传附件卡片、右侧文件预览和旧上下文 UI 移除；保留模型管理、失败恢复及既有对话与工作流能力 |
| `src/styles/app.css` | v0.9.38 未发布源码 | 文件卡片、右侧预览、键盘焦点和交互状态；保留模型、错误提示、消息、自动化和更新样式 |
| `src/types.ts` | v0.9.38 未发布源码 | 通用 `preview.openFile` 桥接类型，以及既有模型目录和对话模型类型 |
| `desktop/main.cjs`、`desktop/preload.cjs` | v0.9.38 未发布源码 | 工作区文件安全预览、连续请求失效保护、规范路径系统打开和独立本地预览分区；保留模型路由、大附件物化与安全桥接 |
| `desktop/services/model-registry.cjs` | v0.9.35 新增，未发布 | 模型校验、独立 Secret key、旧 Key 迁移、脱敏目录和执行路由解析 |
| `desktop/services/cloud-account.cjs` | v0.9.39 新增，未发布 | 设备登录、改密、会话恢复、账号/额度/用量和云端模型目录 |
| `desktop/services/cloud-gateway-proxy.cjs` | v0.9.39 新增，未发布 | 回环鉴权、固定网关路径、服务端令牌转发和逐请求幂等键 |
| `desktop/services/harness.cjs` | v0.9.37 未发布源码 | 每次运行独立 Harness Home；目标严格绑定本次 `runsRoot/run-*`，遇到 Windows Junction 只删除链接本身 |
| `desktop/services/store.cjs` | v0.9.35 未发布源码 | 模型目录、旧设置迁移和对话模型持久化 |
| `desktop/services/prompts.cjs`、`attachments.cjs` | v0.9.38 未发布源码 | 通用生成文件路径规则、附件暂存 Junction/canonical 边界，以及既有预览预算、头尾节选、分段读取和严格提取上限 |
| `desktop/services/preview.cjs` | v0.9.38 未发布源码 | 工作区 `realpath`/Junction 守卫、安全 Markdown/文本/图片/文件信息渲染 |
| `desktop/services/automation.cjs` | 已发布 | 计划规则、下次执行时间和对话定时意图解析 |
| `desktop/services/updater.cjs` | v0.9.34 已发布 | GitHub Releases 检查、下载和状态通知；安装时退出当前应用但不自动重启 |
| `build/installer.nsi`、`build/stable-install-section.nsh` | v0.9.34 已发布 | 原生百分比进度、关闭保护、完成后关窗、健康检查、失败回滚与持久错误页 |
| `.github/workflows/release.yml` | v0.9.39 路径已同步，未运行 | 手动预检不发布；正式标签成功后才上传完整安装包、轻量包、blockmap 和轻量 `latest.yml` |
| `tests/` | v0.9.39 共 141 项通过 | 在既有回归基础上新增账号、改密、恢复、代理鉴权、唯一幂等键和桌面云端契约测试 |

**接手约束：不要无故重置、覆盖或回退以上已发布功能。** 后续开发应从 `origin/main` 继续。

## 8. 本轮验证结果

验证时间：2026-09-01；当前源码验证对象为发布准备中的 `v0.9.39`，正式发布资产验证基线仍为 `v0.9.34`。

### 8.0 v0.9.39 源码与本地打包门禁（未发布）

| 检查 | 结果 |
| --- | --- |
| Node 自动化测试 | `npm test` 141/141 通过，0 失败；新增账号、强制改密、会话恢复、回环代理、唯一幂等键、桌面 IPC/UI 和云端模型路由回归 |
| TypeScript 类型检查 | `npm run typecheck` 通过 |
| Vite 生产构建 | `npm run build` 通过；主 JavaScript chunk `668.77 kB`，保留既有大于 500 kB 的非阻塞警告 |
| 完整安装包 | `npm run dist` 通过；`release-0.9.39/Stable-Setup-0.9.39-x64.exe` 为 `283855390` bytes，SHA-256 `51FA525314B5E76F6F97DEF8FDCEA22A1FF02E93F932C8DAF95571CAA0398882` |
| 轻量更新包 | `npm run dist:update` 通过；`release-0.9.39-update/Stable-Update-0.9.39-x64.exe` 为 `115329902` bytes，SHA-256 `008AD118D82CCF8C628D962113EC374EE63C0039DCC2490314DEFDC3A4D80FD2`；`latest.yml` 为 `354` bytes，SHA-256 `E9606DE6143178E9CC3E2B61A98D8ED12257B5762AAEC9D4299CC36FEFBBD974` |
| 打包后健康检查 | `release-0.9.39/win-unpacked/Stable.exe --stable-update-healthcheck` 静默执行，退出码 `0` |
| GitHub Windows 预检 | Run `33431193467`、提交 `5f20eb47b76fdfc024d298f6ab66b4b11a63dbc9`，总耗时 `22m45s`；测试、两类打包、基线下载、安装器 QA 和证据上传均通过，Release 步骤按手动预检规则跳过 |
| 可见成功更新 | v0.9.31 → v0.9.39；进度单调到 `100|complete|success|0`，未自动重启，快捷方式指向新版，用户数据保留，Runtime 与新版健康检查通过 |
| 可见失败回滚 | 强制健康检查失败后终态 `92|healthcheck_rollback|failed_rolled_back|12`；结果窗口保持打开，旧版 v0.9.31、用户数据与嵌入 Runtime 均恢复 |
| 静默失败回滚 | 安装器进程和状态文件退出码均为 `12`；回滚到 v0.9.31，用户数据保留且 Runtime 健康检查通过 |
| 云端账号与额度 | 登录、临时密码强制改密、令牌轮换、恢复/退出、账号快照、金额额度和近 30 天用量均有主进程及 UI 契约测试 |
| 模型网关安全 | 真实云端 Bearer 不进入渲染页面或 Harness；本机代理拒绝无随机凭据请求，连续两次模型请求产生不同 `Idempotency-Key` |
| 外部可达性 | GitHub 最新 v0.9.34 Release API 返回 200 且发布登录有效；Stable Cloud 已改为公网可达，匿名健康检查为应用 JSON 200，未登录账号接口为应用 JSON 401 |
| 文件卡片 | 生成文件绝对路径、已物化上传附件和本地文件链接共用原生按钮卡片与 `stable:preview:openFile`；“本次上下文”入口、面板和 CSS 已移除 |
| 工作区路径边界 | 工作区内真实文件通过；缺失文件、外部文件、同名前缀兄弟目录、目录冒充文件及工作区内 Junction 指向外部均被拒绝 |
| 本地预览隔离 | 网页与本地文件使用独立 partition；本地文件禁用 JavaScript、联网、权限、下载、新窗口、WebView 和拖放导航，顶层导航不接受 `data:`/`blob:` |
| Electron 文件预览冒烟 | 深色/浅色 1440×900 均显示 v0.9.38、生成文件卡片和 50% 预览栏；上传附件卡片触发 `qa-preview.md`，预览标题正确、`loading=false`，页面无横向溢出 |
| 大附件原件与预览 | 36 万中文字符 TXT 的工作区副本逐字节一致并保留尾部哨兵；8 个大附件全部保留名称与路径，提示词附件段低于 32000 字符 |
| 文件夹与 ZIP 上限 | 标题前缀计入 300000 字符提取预算，两个容器回归均未越界；ZIP 路径遍历拒绝测试继续通过 |
| 失败恢复与布局 | 已覆盖旧 30 万字报错删除、暂存回滚、未接受消息的资源恢复、Composer 内联 `role=alert` 与全局提示顶部定位源码契约 |
| 模型存储迁移 | 旧 `settings.model` 幂等迁移；既有对话绑定迁移模型；重开不重复创建配置 |
| 模型默认与对话隔离 | 新对话快照当前默认；已有对话不随默认变化；单个对话切换持久化；删除非默认模型时引用会话确定性回退 |
| 凭据隔离 | 每模型独立加密 Secret；Bootstrap/SQLite/模型元数据不含明文 Key；旧 Key 复制后保留回退兼容 |
| 运行快照 | 已测试编辑模型和更换 Key 后，已解析路由仍保持旧模型与旧 Key，下一次解析使用新值 |
| Harness 并发 | 两个 fake 子进程实际获得不同 `DSH_HOME`、不同 YAML 和不同 `STABLE_API_KEY`；关闭后只清理运行目录本身 |
| UI/IPC 契约 | 模型管理、默认模型、对话 radio、下一条消息提示、文件卡片、侧栏预览、脱敏目录和 IPC 源码测试通过 |
| Electron Junction 清理 | 实际 Electron host 创建普通和 scoped Junction；清理后 `run-*` Home 消失，两个目标哨兵均保留；其他根下同形目录被拒绝且哨兵保留 |
| 本机 Runtime 修复 | 正式路径 37,847 个文件、730,332,578 bytes；Node v24.14.0、Harness 0.1.0-rc.5；关键入口 SHA-256 为 `C0226687...BC366C62`，`HarnessRunner.ready()` 为 `true` |
| Harness 本地集成 | Electron host 下完整模拟调用连续五次通过：`HARNESS_INTEGRATION_PASS requests=1 events=4`；Runtime 数量、字节、哈希以及三个曾受影响依赖的文件数均保持不变；未连接真实模型或消耗额度 |
| 源码版重启 | 应用版本为 `0.9.39`；本轮两次隔离 Electron Cloud 截图均受当前环境 Chromium Renderer/GPU 子进程启动失败限制，未保持运行，也未计为验收通过 |
| 本轮未执行 | 真实模型服务请求、真实用户文件人工界面验收、标签、正式 GitHub Release 与其他真实设备更新验收 |

### 8.1 v0.9.34 发布门禁

| 检查 | 结果 |
| --- | --- |
| 本地源码门禁 | `npm test` 115/115、`npm run typecheck`、PowerShell AST 解析、定向更新器测试和 `git diff --check` 全部通过 |
| 本地轻量包编译 | `npm run dist:update` 通过，NSIS 可生成 `Stable-Update-0.9.34-x64.exe` 与 blockmap |
| 手动预检工作流 | Run `33312878503`、提交 `8688343`、`21m21s`，测试、两类打包、安装器 QA 和证据上传通过；Release 发布步骤明确跳过 |
| 正式标签工作流 | Run `33313929332`、标签 `v0.9.34`、`19m4s`，所有步骤通过并发布四项资产 |
| 可见成功更新 | 进度日志单调到 `100|complete|success|0`，版本为 `0.9.34`，未自动重启，快捷方式指向新版，用户数据与 Runtime 可用 |
| 可见失败回滚 | 终态 `92|healthcheck_rollback|failed_rolled_back|12`，失败窗口至少保持打开到 QA 主动关闭，旧版恢复为 `0.9.31` |
| 静默失败回滚 | 安装器进程退出码和脚本观测退出码均为 `12`，旧版与嵌入 Runtime 恢复 |
| 发布后核验 | `v0.9.34` 为最新正式 Release，非草稿/非预发布，目标提交正确，四项匿名直链均为 HTTP 200 |
| 轻量包远端复核 | 从公开链接下载后大小 `115322259`；SHA-256 与 GitHub digest 一致，SHA-512 与远程 `latest.yml` 一致 |
| 真实旧设备边界 | 尚未执行；GitHub Hosted Windows 隔离 QA 不能替代另一台设备的网络、UAC、杀毒软件、旧安装残留和真实用户数据验收 |

### 8.2 v0.9.31 历史验证记录

| 检查 | 命令 | 结果 |
| --- | --- | --- |
| TypeScript 类型检查 | `npm run typecheck` | 通过 |
| Node/CJS 语法检查 | `node --check` | 主进程、preload、Harness、预览服务和提示词全部通过 |
| Node 测试套件 | `npm test` | 110 项全部通过，0 失败 |
| Vite 生产构建 | `npm run build` | 通过 |
| Harness 真实集成测试 | `npm run test:integration` | 本轮未执行 |
| Electron 隔离界面验收 | QA 环境变量 + Electron 源码版/打包版 | 通过；任务操作区与输入框无选中背景、阴影和动画 |
| Windows NSIS x64 | `npm run dist -- --publish never` + `npm run dist:update` | 通过；完整安装包、轻量更新包、两类 blockmap 和更新 `latest.yml` 均已生成 |
| 隔离全新安装与启动 | 完整安装器静默安装 + `--stable-update-healthcheck` | 通过；安装退出码 0，Runtime、Harness、卸载器和健康检查均通过 |
| 隔离成功更新 | 旧目录 + 轻量更新包 + 持久 Runtime | 通过；约 6.98 秒，安装与健康检查退出码均为 0，正常启动后清理旧备份 |
| 隔离失败回滚 | 强制健康检查失败 | 通过；约 8.05 秒，退出码 12，旧 EXE 与 Runtime 完整恢复 |
| 隔离 QA 卸载 | 自定义目录 + `_?=` 精确路径 | 通过；安装与卸载退出码均为 0，应用目录和持久 Runtime 均清理 |
| GitHub Actions 发布 | `Release Stable` | 通过；运行 `33298757511`，四项正式资产上传完成 |
| 更新资产匿名访问 | GitHub Release 直链 | 安装包、blockmap、`latest.yml` 均返回 HTTP 200 |
| 本地升级测试基线 | 隔离构建 `0.9.26` 测试包 | 类型检查、101 项测试和构建通过；内部版本与更新源已核对 |
| Electron 人工交互验收 | `npm start` | 尚待用户本地手动测试拖拽、联网和剪贴板体验 |
| Team 双设备实测 | `npm run start:team-dev` | 本轮未执行 |

生产构建存在一个非阻塞警告：主 JavaScript chunk 约 `650.99 kB`，超过 Vite 默认 `500 kB` 警告阈值。功能不受影响，但后续可通过动态导入或 `manualChunks` 拆包。

## 9. 当前发布物

### v0.9.34 当前正式发布资产

- GitHub Release：`https://github.com/LLucas88/Stable/releases/tag/v0.9.34`
- 发布提交：`868834327f0679bbbe9ced3686299d8a4f574291`
- 完整安装包：`Stable-Setup-0.9.34-x64.exe`，`283847754` bytes，GitHub 远端 SHA-256 `B4D0C41E03695EBE7676771B916D9D8CB1C2FFE0BAB230B8A7731ED731752529`。
- 轻量更新包：`Stable-Update-0.9.34-x64.exe`，`115322259` bytes，GitHub 远端 SHA-256 `884F72C4F326E51F9DA4CE1BB2D021C3271C825F2CAFA8F22274F171CE33F870`。
- 轻量更新包 SHA-512：`0svovDGQGqki/aVGvny3GBvae3JRPtoIKxrWOLrLVDkJseJelKkJaafpREuuq3WM99ypeNVXuCyro+6kJZtcRQ==`；已用公开下载文件复算并与远程 `latest.yml` 比对一致。
- 更新 blockmap：`Stable-Update-0.9.34-x64.exe.blockmap`，`120373` bytes，GitHub 远端 SHA-256 `E9B74553EDA1373ADA30A4DC32DA7771B25175B70D632049CED4413902B5ECA7`。
- 更新清单：`latest.yml`，`354` bytes，GitHub 远端 SHA-256 `7C4C4F6FD1DB718D8F6418AF1199D5A2D67214AAF26AEBF72787DA179338ACF9`。
- 远程 `latest.yml`：`version: 0.9.34`，`path: Stable-Update-0.9.34-x64.exe`，`size: 115322259`，SHA-512 与上项一致。
- 四项资产的匿名下载链接均返回 HTTP 200。
- 正式发布工作流：`https://github.com/LLucas88/Stable/actions/runs/33313929332`
- 安装器 QA 证据：Run artifact `stable-update-installer-qa`，包含成功、可见失败、静默失败的完整阶段日志和 `result.json`。

### v0.9.31 历史正式发布资产

- 完整安装包：`release-0.9.31/Stable-Setup-0.9.31-x64.exe`，`283844927` 字节，SHA-256 `2AF4588409F51F7D207624E6877AA0D23836006D5544D652E7612CCEDD3AD218`。
- 轻量更新包：`release-0.9.31-update/Stable-Update-0.9.31-x64.exe`，`115319891` 字节，SHA-256 `9D0169F6789BB4F1D47CAA9644338C88064075680D35B8AF26009DB493215DB9`。
- 更新清单：`release-0.9.31-update/latest.yml`，明确指向 `Stable-Update-0.9.31-x64.exe`。
- 正式发布必须同时上传完整安装包、轻量更新包、轻量更新包 blockmap 和轻量目录中的 `latest.yml`；不得用完整安装包的 `latest.yml` 覆盖。

- GitHub Release：`https://github.com/LLucas88/Stable/releases/tag/v0.9.31`
- 完整安装包：`Stable-Setup-0.9.31-x64.exe`
- 远程完整安装包：`283846048` bytes，SHA-256 `C0FAAEB9BF1C1F4033DDC2E97AB75C4A9D85D6ABFDC0AA787355FB192355F528`
- 轻量更新包：`Stable-Update-0.9.31-x64.exe`
- 远程轻量更新包：`115320491` bytes，SHA-256 `D6B535D7FAB4F3E5660695681636AF31CB54014DF0371264E95AABEFF2CD4F74`
- 更新资产：`Stable-Update-0.9.31-x64.exe.blockmap`、`latest.yml`
- 远程 blockmap：`120382` bytes，SHA-256 `852AC618DA772813D766B8E5144F5CAAE91FF32E2C6E6AC7EAF8474C54C67CAB`
- 远程 `latest.yml`：`354` bytes，SHA-256 `03F93FBF42C62439BB4F2B0566B7B50BF7F2AD3255BC37839B3F9F1FB8CE973B`；明确指向轻量更新包。
- 打包输出配置：`release-0.9.31`、`release-0.9.31-update`
- 发布工作流：`https://github.com/LLucas88/Stable/actions/runs/33298757511`
- 运行时预发布：`runtime-v1/stable-runtime-win-x64.zip`
- 运行时压缩包大小：`230,630,438` bytes
- 运行时 SHA-256：`84FA5C25470A23BE21AB3027E0471E0185D1E4E673A12B173157EEA74A5C64F0`

本地测试交付物：

- 目录：`release-update-test-0.9.26/`
- 安装包：`Stable-Update-Test-Base-0.9.26-x64.exe`
- 文件大小：`283,892,415` bytes
- SHA-256：`847F01B63ACE91AC4C646481563EC3595F951DB031B2A1199695044283AAB6EA`
- 测试说明：`release-update-test-0.9.26/TEST_GUIDE.md`
- 校验文件：`release-update-test-0.9.26/SHA256SUMS.txt`
- 用途：只用于测试设备验证 `0.9.26` 自动发现、下载并重启升级到公开 `0.9.27`
- 发布边界：测试包未提交 Git、未上传 GitHub、未使用商业代码签名证书

注意：公开 Release、更新元数据和测试基线安装包已验证；真实设备上的下载、重启安装和用户数据保留闭环尚未完成。

## 10. 已发布 P0 能力详细状态

以下需求已经与用户确认并完成源码及自动化测试；仍需真实 GUI/集成验收：

### P0：执行计时器

- 位置：仅在“执行过程”折叠栏上方。
- 格式：不足一分钟只显示秒；满一分钟显示分钟和秒；满一小时显示小时、分钟和秒。
- 示例：`49秒`、`4分钟49秒`、`1小时4分钟49秒`。
- 计时范围：从本次 Agent 执行开始到完成、失败或停止。
- 当前结果：已实现并补中文时间边界测试。

### P0：复制用户消息到主输入框

- 每个用户消息气泡的右下方增加复制按钮。
- 复制消息文本以及该消息关联的附件/引用资源。
- 写入页面底部主输入框，但不自动发送。
- 若主输入框已有草稿，按最终确认规则直接覆盖；紧接着按 `Ctrl+Z` 可恢复原文字、附件和引用资源。
- 当前结果：已实现；新消息保存资源 ID/附件路径，旧记录按名称尽力回查。

### P0：浏览器与工作区文件对话侧栏预览

- 删除输入框眼睛按钮和独立预览 CLI；网页链接、Agent 生成文件卡片、已物化上传附件卡片和本地文件链接均可触发。
- 使用 Electron 原生安全 `WebContentsView` 嵌入当前窗口右侧，不使用 computer-use。
- 首次打开默认占对话工作区 50%，支持鼠标/触摸拖动分隔线，也支持方向键调宽。
- 浏览网页仅允许无内嵌账号密码的 `http:` / `https:`；本地文件必须真实存在，且工作区和目标经 `realpath` 后仍保持包含关系，不能用 Junction/符号链接越界。
- 本地文件预览使用独立 partition 并禁用 JavaScript、联网、权限、下载、新窗口、WebView 与拖放导航；未知格式显示安全文件信息，不交给外部程序执行。
- 当前结果：深色/浅色隔离 Electron 验证默认比例为 `0.5`，上传附件卡片可触发加载完成的 Markdown 预览，生成文件卡片可见；真实外网页面和多格式真实用户文件仍待人工验收。

### P0：联网搜索凭据链

- Stable 模型服务继续以 `STABLE_API_KEY` 作为主模型凭据。
- 只有当前模型的 `providerId` 为 `deepseek` 时，才把该模型 Key 同步映射为子进程的 `DEEPSEEK_API_KEY`；非 DeepSeek 模型不能把自己的 Key 冒充搜索凭据。
- 若宿主环境单独配置了 `DEEPSEEK_API_KEY`，非 DeepSeek 模型运行会保留该独立搜索凭据；所有注入都只存在于短生命周期子进程，不写入 YAML 或全局环境。
- 当前结果：代码链和自动化测试通过；未使用用户真实密钥发起联网查询，仍需本地集成验收。

### P0：对话复制、回答宽度与表格交互

- 用户消息复制改为右下角纯图标按钮，不显示文字注释。
- 助手文字、代码和表格的内容宽度与底部输入框统一。
- 表格悬停不再产生抬升或阴影动画；右上角出现纯图标复制按钮，只复制 Markdown 表格内容。
- 当前结果：源码与自动化测试通过；对话布局已在 1440×900 隔离 Electron 中验证无横向溢出。

### P0：全局 Agent 对话提醒

- 在“设置 → 模型服务”下方增加设置区域。
- 编辑 `%APPDATA%\stable-desktop\AGENTS.md`。
- 文件不存在时创建，保存后只影响之后启动的新对话/新任务。
- 用途：向模型提供适用于本机所有 Stable 对话的额外说明和上下文。
- 当前结果：已实现读取、创建、保存和新任务提示词注入；正在执行的任务不受保存动作影响。

## 11. 还剩什么与当前卡点

### 11.1 未完成事项

1. 在出现过远程更新问题的真实旧版本设备上完成升级到 `v0.9.34`，记录检查、下载、Stable 退出、安装百分比、安装窗口关闭和重新点击图标后可用的实际耗时。
2. 验证安装窗口在达到 100% 且安装完成前不会消失；完成后不自动重启 Stable，重新点击桌面或开始菜单图标可打开 `v0.9.34`。
3. 验证升级后桌面快捷方式与安装目录有效，原有对话、定时任务、模型设置和资源记录仍存在，Harness 能从持久或兼容 Runtime 正常启动。
4. 若真机出现失败页，记录百分比、中文状态、错误码和“旧版本已恢复”结果；若仍出现“软件消失”，保留安装目录、进程、快捷方式和 Runtime 现场再定位。
5. 使用真实模型服务密钥验证 Web Search，并在用户网络中验证真实网页侧栏预览。
6. 人工复测主题、窗口缩放、分栏拖拽、剪贴板、中文输入法、焦点、文件卡片和定时任务触发。
7. 使用两台设备完成 Team 对话快照的接受、拒绝和继续对话验收。
8. 先在可正常启动 Chromium Renderer/GPU 子进程的桌面环境中启动源码版 Stable，再连续发起至少两个普通任务和一次大附件任务；重点确认第一个任务结束后第二个仍可运行。界面端到端通过后，再决定是否清理 `D:\Codex\2026-08-19\Stable-0.9.1-runtime-backups\runtime-broken-20260831-1029` 与 `runtime-broken-20260831-1131` 两份备份。
9. 决定 Team 远程任务与协作后端是保留并文档化，还是删除残留代码与测试。
10. 使用至少两个真实 OpenAI Chat Completions 兼容服务验证新增、编辑、设默认、对话切换、并发请求和 API Key 不串线；运行中切换时确认当前任务保持旧模型、下一条使用新模型。
11. 用复制的 v0.9.34 用户数据完成一次迁移启动与重启，确认旧对话、旧单模型配置和旧加密 Key 只迁移一次且仍可使用。
12. 在 Electron 桌面中用单个超过 30 万字符文件和多个合计超过 30 万字符附件做人工验收，确认可发送、错误不遮挡控件，并让 Agent 通过路径读取预览之外的尾部内容。
13. 用真实生成文件和真实上传附件覆盖 Markdown、HTML、图片、TXT/CSV/JSON、DOCX、XLSX、PDF、ZIP/文件夹与未知格式；确认卡片、切换对话关闭、连续快速打开、侧栏拖拽、静态 HTML 限制和工作区外/UNC/Junction 拒绝符合预期。
14. 为删除或清空对话后的 `.stable/attachments/<conversationId>` 孤儿副本制定保留期与安全回收策略；当前删除数据库消息不会主动删除已提交附件原件。
15. 在 Stable Cloud 管理后台配置至少一个可用模型、价格和服务端供应商 Key；执行一次成功结算、一次额度不足 429、一次取消/异常请求对账。

### 11.2 卡点

1. **v0.9.34 真机结论依赖其他设备**：远程资产和 GitHub Hosted Windows 的可见成功、可见回滚、静默回滚均已验证，但真实旧安装的网络、权限、UAC、杀毒软件、快捷方式和用户目录状态只能在用户设备上确认。
2. **真实联网依赖用户环境**：工作区 Markdown 文件卡片已通过隔离 Electron 验证，但真实网页和 Web Search 需要有效模型密钥及可访问外网的网络。
3. **运行时不进入 Git**：其他开发环境和完整正式打包必须下载 `runtime-v1`，或用 `STABLE_DSH_RUNTIME` 指向有效入口；轻量更新包本身不能修复一个已经缺失或损坏的旧 Runtime。本机源码 Runtime 已恢复，但损坏备份需保留到人工任务验收完成。
4. **安装包尚未签名**：Windows 可能拦截或显示“未知发布者”，正式扩大分发前需要决定是否购买并配置代码签名证书。
5. **GitHub 是当前更新单点**：仓库公开性、Release 资产命名、`latest.yml` 或 GitHub 网络可用性变化都会影响客户端发现和下载更新。
6. **Team 后端去留尚未决定**：前端已移除远程任务/审批模块，后端仍有 `team-coordination`、远程任务和子 Agent 协作代码。
7. **多模型首版只覆盖 OpenAI Chat Completions 兼容接口**：Azure 专用认证头、OAuth、供应商私有协议或额外查询参数尚未实现，不能仅凭可保存 Base URL 就宣称支持。
8. **v0.9.38 只有源码与隔离 UI 证据**：尚未打包、发布、使用真实服务商联调或执行真实用户文件的桌面人工验收，线上用户仍在使用 v0.9.34。
9. **真实供应商模型与 Key 尚未配置**：D1 账号与额度已上线，Sites 公网门禁也已解除，但生产模型目录/服务端密钥没有验收证据；在此之前不能把 v0.9.39 视为可正常调用模型。

### 11.3 技术风险

- 全局 `AGENTS.md` 会影响此主机后续全部聊天，必须清楚展示作用范围，不能与当前对话临时提示混淆。
- 浏览器/文件预览 IPC 若没有协议、realpath 路径、导航、网络和窗口数量限制，会扩大本地攻击面；当前本地 HTML 因安装版保留全局 `--no-sandbox` 而明确禁用 JavaScript，不能把它表述为完整交互式网页运行环境。
- 用户消息附件复制若只复制显示文本而不复制资源 ID，会导致界面看似成功、实际 Agent 读不到附件。
- 已提交附件当前随工作区保留；删除或清空对话只处理数据库记录，长期使用可能积累不再被消息引用的附件副本，后续需要带审计和防 Junction 的回收机制。
- 当前 Git 发布标签、线上最新 Release 和自动更新元数据均为 `v0.9.34`；工作区源码已递增到发布准备中的 `v0.9.39`，不能把本地版本号当成线上可下载版本。
- Windows 安装包尚未签名，测试设备和正式用户可能看到“未知发布者”。
- 当前远程更新源依赖公开 GitHub Releases；仓库改回私有或资产命名变化都会使匿名更新失效。
- v0.9.34 轻量包依赖旧安装中存在可复用或可迁移的 Runtime，首次跨版本更新前仍需保留旧安装完整性。
- 定时任务只在 Stable 进程运行时触发，软件关闭期间不会由系统后台唤醒。
- Team 对话只应发送“发送时刻的可见问答和附件元数据”，不能后台持续同步后续消息。

## 12. 踩过的坑与处理原则

1. **把计划误当成完成**：README、CHANGELOG、截图和讨论只能作为线索；交接时必须以当前源码、测试和真实运行结果为准。
2. **旧版本和新版本混用**：本地保留多个 `release-0.9.*` 目录，启动时容易误开旧版。开发测试应始终从当前源码目录运行，并确认左下角版本号。
3. **脚本只导入入口文件**：会丢失 `run.ps1`、Node 包、配置或旁路文件。完整脚本必须以文件夹或 ZIP 导入，并保持相对结构。
4. **符号链接与依赖重建**：目录链接可以在私有包中重建，但文件链接需要拒绝或转为真实文件，避免包不完整和越界访问。
5. **交互脚本被误判为卡死**：半自动脚本需要保持 stdin 打开，识别控制台问题，并允许 Agent 自动回答；不能只按普通一次性脚本处理。
6. **输出状态与真实文件脱节**：只有验证目标文件确实存在后才能显示“结果已输出”，随后才能创建出口节点。
7. **默认上下文过大**：工作区清单、全部数据和全部知识库会浪费模型上下文并暴露无关信息，必须坚持按需引用。
8. **静态测试不等于用户可用**：类型检查和源码断言通过后，还要测试中文输入法、焦点、画布拖拽、深浅主题、窗口缩放和真实文件打开。
9. **连接线动画方向/长度容易与视觉需求不一致**：动画必须从上游到下游，发光段应覆盖足够长度，速度和主题颜色需按真实画面验收。
10. **过早打包**：本地测试阶段不应反复生成正式安装包。只有源码和 GUI 验收稳定、用户明确要求发布时再打包。
11. **覆盖用户数据**：安装、升级和卸载默认保留 Electron 用户数据；Git、安装包和测试产物不能包含 API Key、用户附件或运行日志。
12. **Electron Builder 异步发布不可靠**：最初的 Release 工作流显示成功，但只上传了 blockmap，主安装包和 `latest.yml` 缺失。发布流程必须先用 `--publish never` 完成本地构建，再用 `gh release upload` 显式上传固定资产，并在同一步读取 Release 资产名逐项复核。
13. **正式旧版本无法测试新增更新器**：原始 `v0.9.26` 不含更新代码，无法靠它验证自动更新。测试基线必须使用新代码、旧内部版本号构建，并明确标注为测试专用，避免与真实历史版本混淆。
14. **Windows 临时工作树可能触发长路径限制**：清理隔离构建目录时，`git worktree remove --force` 曾因依赖目录路径过长失败。先确认目录与链接目标，再使用支持长路径的 PowerShell 清理；不要对未解析路径执行递归删除。
15. **未签名安装包会触发系统提示**：远程更新功能可用不代表发布者身份已建立。测试时应先核对 SHA-256，正式分发前应评估 Windows 代码签名证书。
16. **版本号必须随需求递增**：每次修复或新增需求完成并通过验证后，必须同步递增应用版本号；即使暂不打包或发布，也不能继续沿用旧版本号。
17. **完整安装包不适合作为日常远程更新包**：Runtime 约有 3.8 万个文件，完整安装实测约 17 分钟。远程更新必须使用不包含 Runtime 的轻量包，完整包只用于首次安装或 Runtime 修复。
18. **先移除旧程序会制造“软件消失”**：如果新版本尚未完整展开，桌面快捷方式和安装目录会在长时间窗口内不可用。正确顺序是先在相邻暂存目录准备新版本，再原子切换，并在健康检查失败时回滚。
19. **完整包与轻量包的 `latest.yml` 不能混用**：两个输出目录都会生成更新元数据；正式 Release 必须上传轻量目录中的 `latest.yml` 和对应 blockmap，否则客户端会重新下载慢速完整包或无法差分更新。
20. **本地包与 CI 包的哈希不能互相代替**：构建时间戳和打包环境会导致正式远程资产与本地产物的大小、哈希不同。状态文件必须分别记录本地验证值和远程 Release 值。
21. **流水线成功不等于客户端可更新**：还要核验 GitHub 最新 Release 指向目标版本、四项资产名称完整、匿名直链返回 HTTP 200，并读取远程 `latest.yml` 确认路径和大小。
22. **隔离升级不等于真实旧设备升级**：隔离环境证明暂存、切换、健康检查和回滚逻辑能够工作，但杀毒软件、权限、旧安装残留和真实用户数据仍需要真机验收。
23. **不要为更新进度另造不稳定的 Win32 窗口**：早期自建窗口在 GitHub Hosted Windows 安装器中触发访问冲突。应复用 NSIS InstFiles 页的原生进度条 `1004` 和状态文本 `1006`。
24. **NSIS 的 `FileOpen ... a` 不会自动把指针移到文件末尾**：持久进度日志必须在写入前执行 `FileSeek ... END`，否则每次会覆盖前一条记录，无法证明百分比单调推进。
25. **NSIS `Abort` 会进入持久失败页并恢复默认 Caption**：失败结果复检只应精确允许 `Stable Setup`，同时继续严格检查同一进程、可见进度条、状态控件、百分比、回滚文案和错误码；运行中及首次终态仍要求版本化更新标题。
26. **PowerShell 重新获取的 `Process` 对象可能读不到退出码**：等待安装器退出前应先访问并缓存进程 Handle，再读取 `ExitCode`；否则真实退出码 `12` 可能被误判为空。
27. **失败标签不能自动成为公开版本**：发布工作流先允许 `workflow_dispatch` 完整预检但禁止上传，只有成功推送的版本 tag 才能执行发布步骤；`v0.9.32`、`v0.9.33` 因此没有留下不完整 Release。
28. **并发模型不能共享 Harness 配置文件**：若多个对话覆盖同一 `settings.yaml`，即使主进程选对模型，也可能在子进程读取前被另一任务改写。每次运行必须拥有独立 `DSH_HOME`，且任务启动时冻结路由与 Key。
29. **Runtime 入口存在不代表依赖完整**：本次故障中 `node.exe` 和外层 `dsh/package.json` 仍正常，但 Harness 入口及 194 个依赖清单已缺失。修复必须从已验证的同版本完整来源做整体暂存、数量/字节/哈希复验和可回滚切换；不要针对旧本机 tarball 路径直接执行依赖重装，也不要只补 `bin.js`。
30. **Electron 与普通 Node 对 Windows Junction 的递归删除语义可能不同**：普通 Node 集成通过不能覆盖 Electron 宿主风险。临时目录清理必须使用 `lstat`、只解除链接、绑定确切 `runsRoot`，并在项目 Electron 下以外部哨兵做回归。
31. **字符串前缀不能证明文件仍在工作区**：`workspace-copy`、大小写、`..`、Junction 和符号链接都可能绕过简单字符串判断。本地文件卡片、预览和系统打开必须统一以工作区与目标的 canonical realpath 做包含校验，并用校验后的路径执行后续操作。

## 13. 下一步执行顺序

### P0：完成真实设备更新验收

1. 在出现过远程更新问题的旧版本测试设备备份 `%APPDATA%\stable-desktop`，记录原安装目录、桌面快捷方式目标和升级前版本。
2. 创建一条测试对话、一个测试定时任务和一项资源记录，作为用户数据保留样本。
3. 启动 Stable 并检查更新，确认客户端发现 `v0.9.34`，后台下载完成后显示安装更新入口。
4. 点击安装更新，从 Stable 退出开始计时，记录 UAC、安装窗口、百分比、中文状态、进程、安装目录和快捷方式在各阶段的状态。
5. 确认安装窗口在真正完成前保持打开，达到 `100%` 后才自行关闭；确认 Stable 不自动重启，然后重新点击桌面或开始菜单图标。
6. 确认界面显示 `v0.9.34`，桌面快捷方式可用，安装目录中不存在半成品暂存目录或长期残留的旧版本备份。
7. 确认 `%LOCALAPPDATA%\stable-desktop\runtime-v1` 存在且 Harness 能正常运行，并检查对话、定时任务、模型设置和资源记录是否保留。
8. 将设备、耗时、是否出现 UAC、逐阶段百分比、成功或失败结果、重新点击图标后的版本和数据保留结果补回本状态文件。

### P0：处理真机更新异常

1. 若 Stable 再次长时间消失，先保留现场，不重复安装或删除目录。
2. 按“下载 → 暂存解压 → Runtime 迁移 → 目录切换 → 健康检查 → 回滚或完成关窗”定位失败阶段。
3. 对照安装目录旁的 `.__stable_` 暂存或备份目录、持久 Runtime、快捷方式目标和正在运行的 Electron/安装器进程，判断是仍在处理还是已经失败。
4. 修复若涉及业务代码或安装脚本，按固定规则从当前开发版递增新版本，补自动化测试与托管成功/失败路径，再创建新的 Release；不得覆盖已发布的 `v0.9.34` 标签和资产。

### P0：完成真实功能验收

1. `[已完成]` Stable Cloud Sites 已调整为公网可达；托管层不再拦截，应用自身未登录接口返回 JSON 401，管理员页面和所有业务数据仍受账号权限保护。
2. 在云端配置至少一个真实模型、价格和服务端 Key，验证模型目录、成功调用与实际金额结算；把账号额度调到不足，再确认请求在调用供应商前返回 429。
3. 使用两个云端模型验证对话选择、两个对话并发不串线，以及运行中切换时当前任务保持启动快照、下一条使用新模型。
4. 验证用户消息覆盖草稿、资源恢复和 `Ctrl+Z` 完整撤回。
5. 验证对话内真实网页、生成文件卡片、上传附件卡片、多种工作区文件、工作区外/Junction 拒绝、关闭侧栏和自由拖拽比例。
6. 验证全局 `AGENTS.md`、定时任务和两台设备 Team 对话快照。

### P0：完成验证

1. `npm run typecheck`
2. `npm test`
3. `npm run build`
4. `npm run test:integration`（需要有效模型配置和本地 Harness）
5. `npm start` 进行深色/浅色主题、输入法、焦点、侧栏拖拽、联网搜索、剪贴板、生成文件卡片和上传附件文件预览测试。
6. `npm run start:team-dev` 进行双设备对话快照接受/拒绝测试。

### P1：收敛技术债

1. 决定是否保留 Team 远程任务/协作后端；若保留，隐藏入口与权限边界需文档化；若不保留，删除死代码和对应测试。
2. 对 `src/App.tsx` 做按页面拆分，降低单文件复杂度。
3. 对主 JavaScript chunk 做代码分割，消除超过 500 kB 的构建警告。
4. 将“源码结构断言”和“真实行为测试”分层，避免只靠正则测试判断功能完成。

### P2：准备下一版本

1. 在真实设备升级验收完成后，将设备结果、耗时、失败阶段或成功证据记录到 `PROJECT_STATUS.md`；若没有代码变化，只更新文档，不递增应用版本。
2. 每次修复或新增需求完成并通过验证后立即递增应用版本号，不以是否打包或发布为前提。
3. 创建下一版本标签前，同时生成并复核完整安装包、轻量更新包、轻量包 blockmap 和轻量目录中的 `latest.yml`。
4. 发布后再次检查 GitHub 最新 Release、四项资产匿名直链和远程 `latest.yml`，不能只看 GitHub Actions 绿色状态。
5. 正式扩大分发范围前评估 Windows 代码签名，降低“未知发布者”提示和更新安装拦截。

## 14. 关键文件地图

| 文件/目录 | 用途 |
| --- | --- |
| `src/App.tsx` | 主应用、对话、设置、Team、执行过程等主要 UI |
| `src/WorkflowStudio.tsx` | 工作流画布、节点、连线、运行与出口节点 |
| `src/styles/app.css` | 全局组件和页面样式 |
| `src/styles/tokens.css` | 主题与设计 Token |
| `src/types.ts` | 前端状态、IPC 与 trace 类型 |
| `desktop/main.cjs` | Electron 主进程与 IPC 注册 |
| `desktop/preload.cjs` | 渲染进程安全桥接 |
| `desktop/services/model-registry.cjs` | 模型配置校验、脱敏目录、独立加密凭据和执行路由解析 |
| `desktop/services/harness.cjs` | Harness 启动、事件解析和 Agent 执行 |
| `desktop/services/store.cjs` | 本地状态持久化与迁移 |
| `desktop/services/automation.cjs` | 定时规则校验、下一次时间计算和对话建议解析 |
| `desktop/services/updater.cjs` | GitHub Releases 更新状态机、后台下载与重启安装 |
| `desktop/services/update-maintenance.cjs` | 应用成功启动后清理版本化暂存目录与旧备份 |
| `build/installer.nsi` | 完整安装、轻量更新和卸载共用的自定义 NSIS 入口 |
| `build/stable-install-section.nsh` | Runtime 迁移、相邻暂存、目录切换、健康检查与失败回滚 |
| `build/update-builder.config.cjs` | 不包含 Runtime 的轻量更新包构建配置 |
| `.github/workflows/release.yml` | 标签推送后的 Windows 自动构建与 Release 发布 |
| `release-update-test-0.9.26/TEST_GUIDE.md` | `0.9.26 → 0.9.27` 真实设备更新测试步骤与通过标准 |
| `release-update-test-0.9.26/SHA256SUMS.txt` | 本地测试安装包完整性校验 |
| `desktop/services/importers.cjs` | 文件夹、ZIP 和脚本包导入 |
| `desktop/services/script-runner.cjs` | 脚本启动、stdin、停止和超时控制 |
| `desktop/services/workflow-graph.cjs` | DAG 校验、并行分支和布局 |
| `desktop/services/workflow-output.cjs` | Markdown、HTML、Excel、PPT 输出 |
| `desktop/services/team-conversation.cjs` | Team 对话快照序列化和接收决定 |
| `desktop/services/team-network.cjs` | Team 设备发现与本地消息中继 |
| `tests/` | Node 测试、结构回归和行为验证 |
| `runtime/` | 本地 Node 与 DeepSeek Harness 运行时；不进 Git |
| `release-0.9.31/` | `v0.9.31` 完整首次安装包、本地 blockmap、`latest.yml` 与解包目录 |
| `release-0.9.31-update/` | `v0.9.31` 轻量更新包、对应 blockmap 和正式上传使用的 `latest.yml` |

## 15. 本地开发与验收命令

```powershell
cd D:\Codex\2026-08-19\Stable-0.9.1
npm run typecheck
npm test
npm run build
npm start
```

双设备 Team 开发测试：

```powershell
npm run start:team-dev
```

正式打包前：

```powershell
npm run typecheck
npm test
npm run test:integration
npm run dist
npm run dist:update
```

## 16. 交接检查清单

- [x] 本次更新前已阅读本文件，并检查 `git status --short`、当前版本和近期提交。
- [x] v0.9.38 源码改动保留多模型、大附件、Runtime 修复与已发布 P0 能力；本轮尚未提交、打包或发布。
- [x] `v0.9.30` 本地安装包、blockmap 和 `latest.yml` 已生成并完成一致性验证。
- [x] `v0.9.30` 远程安装包、blockmap 和 `latest.yml` 已发布并完成匿名访问复核。
- [x] `v0.9.31` 本地完整安装包、轻量更新包、blockmap 和更新 `latest.yml` 已生成并通过隔离验证。
- [x] `v0.9.31` 标签、主分支与远程 Release 已创建并完成四项资产匿名访问核验。
- [x] `v0.9.34` 本地 115 项测试、类型检查、PowerShell 解析和轻量安装包编译已通过。
- [x] `v0.9.34` 手动预检与正式标签工作流均通过可见成功、可见失败和静默失败安装器 QA。
- [x] `v0.9.34` 为线上最新正式 Release；四项资产、远端 SHA-256、匿名 HTTP 200 和 `latest.yml` SHA-512 已核验。
- [x] `v0.9.35` 多模型源码已通过 125 项测试、类型检查和生产构建；明确记录为未发布状态。
- [x] `v0.9.36` 大附件源码已通过 129 项测试、类型检查、生产构建和关键脚本语法检查；明确记录为未发布状态。
- [x] `v0.9.37` Harness 清理已通过 130 项测试、类型检查、生产构建、Electron Junction/越界哨兵回归和五次 Electron-hosted 集成；本机 Runtime 与三个依赖保持完整，两份损坏目录已保留为回滚备份。
- [x] `v0.9.38` 文件卡片、上传附件预览、上下文面板移除与 realpath 工作区边界已通过 133 项测试、类型检查、生产构建和深色/浅色隔离 Electron 冒烟；最终持久源码重启受当前受管环境的 Chromium Renderer/GPU 子进程启动失败限制，未计为通过。
- [x] `v0.9.39` 云端账号、强制改密、额度/用量、只读模型目录、回环代理和逐请求幂等键已通过 141 项测试、类型检查和生产构建。
- [x] `v0.9.39` 本地完整安装包、轻量更新包、blockmap 和轻量 `latest.yml` 已成功生成；打包后应用健康检查退出码为 0。
- [x] `v0.9.39` GitHub Hosted Windows 手动预检 Run `33431193467` 已通过可见成功更新、可见失败回滚、静默失败回滚、数据保留、Runtime 和快捷方式验证；Release 步骤明确跳过。
- [x] 已将通过预检的 v0.9.39 源码与证据快进合入并推送远程 `main`，公网切换前主分支提交为 `3012a56`。
- [x] 已确认 GitHub 最新正式版仍为 v0.9.34、公开 Release API 可达且当前 GitHub 发布登录有效。
- [x] Stable Cloud Sites 公网入口已获明确批准并完成调整；匿名健康检查 JSON 200、未登录账号接口 JSON 401、首页 200。
- [ ] 尚未配置并验证真实供应商模型、价格和服务端 Key。
- [x] 模型 API Key 只保存在 Windows 加密 Secret 中，Bootstrap、SQLite 模型元数据和 Harness YAML 不含明文 Key。
- [ ] 尚未使用两个真实模型服务完成路由、并发、运行中切换和真实回复验收。
- [ ] 尚未用复制的 v0.9.34 用户数据做真实启动迁移与重启验收。
- [x] 已生成仅供测试设备使用的 `0.9.26` 更新基线安装包、测试说明和 SHA-256。
- [ ] 尚未完成出现过远程更新问题的真实旧版本设备 → `v0.9.34` 的 GitHub 下载、百分比安装、完成后关窗、重新点击图标、版本和数据保留验收。
- [ ] 不把旧 `release-0.9.*` 当作当前源码运行结果。
- [ ] 不把完整工作区、全部数据或全部知识库默认放入模型上下文。
- [ ] 不在没有真实文件验证时显示“输出完成”。
- [ ] 不把类型检查或正则测试当作 GUI 端到端验证。
- [x] 浏览器/文件侧栏已限制协议、realpath 文件边界、脚本、网络、权限、下载、新窗口、WebView、拖放导航和 IPC 参数。
- [ ] 新增全局 `AGENTS.md` 设置时明确作用于本机后续所有聊天。
- [x] v0.9.34 已完成版本号、文档、标签、安装包、更新元数据和 SHA-256 一致性检查；v0.9.39 当前只同步源码版本、测试和未来工作流路径。
