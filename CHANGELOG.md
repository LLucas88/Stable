# Changelog

本项目采用语义化版本号。

## v0.9.39 - 发布准备中

### 账号与云端模型

- 安装版启动时接入 Stable Cloud 内部账号；账号密码只用于登录，不在本机保存，设备会话令牌由 Windows 安全存储加密。
- 支持管理员分发的临时密码首次登录，并在进入工作台前强制修改正式密码；修改后旧会话由服务端统一撤销。
- 设置页显示账号身份、本期金额额度、剩余额度、周期截止日，以及近 30 天请求、Token 和确认消耗。
- 云端模型目录替代安装版的本地供应商 Key 配置；模型、价格和供应商凭据由管理员在 Stable Cloud 统一维护，对话仍可独立切换模型。
- Harness 只连接带随机进程凭据的本机回环代理；真实设备令牌不进入渲染页面或 Harness 子进程。每次模型请求转发时生成独立 `Idempotency-Key`，支持服务端原子预占、结算和超额强制停止。

### 验证与发布边界

- `npm test` 141/141、`npm run typecheck` 和 `npm run build` 通过；新增登录、强制改密、会话恢复、唯一幂等键、本机代理鉴权、渲染桥接和云端模型路由测试。
- 本地完整安装包与轻量更新包均已生成；完整包 `283855390` bytes、轻量包 `115329902` bytes，打包后应用健康检查退出码为 0。该结果不是 GitHub Release 或其他设备验收。
- 公网实测确认 GitHub 最新 Release 仍为 v0.9.34，四项资产可匿名读取，当前 GitHub 账号具备发布权限。
- Stable Cloud 当前托管入口仍是 ChatGPT 所有者登录保护，普通桌面客户端会收到 HTML 401；在改为公网可达且配置真实供应商模型与密钥前，不创建 v0.9.39 Release。
- 当前受管环境的 Electron Renderer/GPU 子进程继续以缺失依赖退出，未把本轮视觉截图计为通过。

## v0.9.38 - 未发布

### 新功能与改进

- 移植历史 v0.1.26 的真实文件卡片能力：Agent 回答中的 Stable 工作区绝对路径会生成文件卡片；已发送并物化到工作区的用户附件也改用同一套可点击卡片。
- 文件卡片、对话中的本地文件链接和上传附件统一打开当前窗口右侧的可调宽预览，不再调用系统外部打开。
- 删除对话顶部“本次上下文”入口、右侧上下文面板及其响应式布局；右侧区域只在用户主动打开文件或网页预览时出现。
- 切换、新建或删除对话时同步关闭上一对话预览；快速连续打开文件时，只有最新请求可以更新侧栏和临时预览路径。
- 通用 Agent 提示词要求任何格式的生成或修改文件都位于当前 Stable 工作区、已经真实写入并完成检查，最终回答中每个已验证绝对路径独占一行；已识别交付任务保留更严格的完成检查。
- 侧栏支持 Markdown、普通文本、CSV/JSON/YAML/XML/日志、Office/PDF 文本提取、图片、静态 HTML，以及不支持格式的安全文件信息页。

### 安全

- 预览、系统打开和“打开所在文件夹”统一对工作区与目标执行 `realpath` 校验；工作区内 Junction/符号链接逃逸、同名前缀兄弟目录、外部路径和不存在路径均会被拒绝。
- 本地 `file:` URL 在路径转换前拒绝主机名，目标路径在任何 `exists`/`realpath` 探测前先做工作区 lexical 边界检查，避免 UNC/SMB 探测。
- 附件暂存目录逐级用 `lstat` 拒绝 Junction/符号链接，复制前后再次核对 canonical 工作区边界；失败回滚不跟随链接递归删除。
- 网页与本地文件使用独立 Electron partition；本地文件预览禁用 JavaScript、联网、权限、下载、新窗口、WebView 与拖放导航，顶层导航只允许当前内部预览文件或工作区内真实文件。

### 验证

- `npm test` 133/133、`npm run typecheck`、`npm run build` 和关键 CommonJS 语法检查通过。
- 新增真实工作区文件、缺失/外部/同名前缀/Junction/UNC 逃逸、附件目标 Junction、HTML 转义、文件卡片、上传附件、提示词路径规则、连续预览和上下文面板移除回归。
- 深色与浅色隔离 Electron 冒烟均显示 v0.9.38、生成文件卡片和 50% 可调预览栏；上传附件卡片可触发 `qa-preview.md`，原生预览状态为加载完成。当前未打包、未发布或使用真实用户文件人工验收。

## v0.9.37 - 未发布

### 修复与改进

- 修复 Electron 43.4.0 在递归删除单次 Harness 临时目录时沿 Windows Junction 清空真实 Runtime 的问题；首次任务成功后，下一次任务不再报“Stable 的 Harness 运行时不完整”。
- Harness 临时目录改为基于 `lstat` 的安全遍历：Junction 与符号链接只解除链接，普通文件和真实子目录才在临时根内删除。
- 清理函数把目标父目录严格绑定到本次 Runner 的 `userData/harness/runs`，只接受其直接 `run-*` 子目录；其他根下外形相同的目录也会拒绝。
- 恢复两次故障中被清空的本地 Runtime，以及被相同 Junction 链路误伤的 `csstype`、`@types/prop-types`、`@types/react` 开发依赖。

### 验证

- 新增 Electron 宿主 Junction 与越界哨兵测试：临时 Harness Home 删除后，普通包和 scoped 包的两个目标哨兵均保留；其他根下同形目录被拒绝且哨兵保留。
- 使用 Electron 宿主连续执行五次本地模拟 Harness 集成流程；五次均通过，且每次结束后 Runtime 仍为 37,847 个文件、730,332,578 bytes，入口 SHA-256 不变。
- 本版本只完成源码与本机测试，未打包、发布、调用真实模型服务或执行桌面 GUI 人工验收。

## v0.9.36 - 未发布

### 修复与改进

- 取消“临时附件正文合计超过 30 万字符即拒绝”的错误准入条件；保留最多 8 个附件、合计 100 MB、文件夹或 ZIP 最多 2000 个文件等物理安全边界。
- 普通附件仍完整复制到隔离工作区；提示词只注入全部附件共享的 24000 字符有界预览，单项最多 6000 字符，并显示截断状态、已提取字符数、完整路径和分段读取说明。
- 修复文件夹与 ZIP 文本预览未将标题前缀计入 300000 字符提取预算的问题。
- 附件预处理硬失败会回滚本批暂存副本；消息尚未被主进程接受时，输入区会合并恢复原草稿、附件和引用，不覆盖用户之后新增的内容。
- 发送错误改为输入框内联提示；全局错误提示移到窗口顶部，避免遮挡权限、模型、能力和发送按钮。

### 验证

- 大文件原件逐字节物化、8 个大附件提示词总预算、文件夹和 ZIP 严格提取上限、失败恢复与 UI 位置均有回归覆盖。
- `npm test` 129/129、`npm run typecheck`、`npm run build` 和主进程关键脚本语法检查通过。
- 本版本尚未打包、发布、运行真实模型服务或执行 Electron 桌面人工验收。

## v0.9.35 - 未发布

### 新功能与改进

- 设置页支持保存、编辑、设为默认和删除多个 OpenAI Chat Completions 兼容模型配置；每个配置拥有独立服务地址与 Windows 加密 API Key。
- 新对话继承创建时的默认模型；已有对话可在输入区独立切换，并明确从下一条消息生效。
- 旧单模型设置、旧加密 Key 和既有对话会幂等迁移，不删除旧配置与旧 Key。
- 模型任务启动时冻结路由与凭据，主 Agent、审批 Agent、自动化意图识别和文件续跑不会被运行中的模型切换影响。
- 每次 Harness 运行使用独立配置目录，避免并发对话覆盖模型路由；非 DeepSeek 模型 Key 不再映射为 `DEEPSEEK_API_KEY`。

### 验证

- `npm test` 125/125、`npm run typecheck`、`npm run build` 通过。
- 本版本尚未打包、发布或使用真实服务商凭据联调。

## v0.9.34 - 2026-08-30

### 修复与改进

- 更新安装器在进入 NSIS 初始化阶段即记录 1% 启动状态，初始化或页面创建失败也能留下可诊断证据。
- 更新窗口复用 NSIS 自带的标准进度条控件，移除自建控件的原生指针与内存操作，修复进度页显示前的访问冲突退出。
- GitHub-hosted 验收不再把启动器进程等同于最终安装进程；外层进程退出后会按安装器路径、启动时间和窗口标题接管新实例，并保留真实退出码与最后进度。
- 成功、回滚和失败诊断统一写入独立证据目录；Release 流程只上传这些小文件，不再打包两套应用与 Runtime 安装树。
- Release 流程支持在打标签前手动执行完整安装验收；只有标签运行会创建 GitHub Release，避免为诊断消耗版本标签。
- Windows 交互脚本测试会等待 PowerShell 完整退出后再清理临时目录，避免慢速 Runner 上的句柄占用误报。
- 保留已推送但未发布的 `v0.9.32`、`v0.9.33` 标签，不移动或覆盖历史标签。

### 验证

- 正式发布前须通过 v0.9.31 → v0.9.34 的可见成功更新、可见失败回滚、静默失败回滚、用户数据保留、Runtime 迁移及不自动重启验收。

## v0.9.33 - 2026-08-30

### 修复与改进

- 修正 GitHub-hosted 更新验收的 v0.9.31 Runtime 基线：官方完整安装包把 Runtime 保存在安装目录，升级成功后才迁移到持久目录。
- 成功路径现在明确验证 Runtime 已迁移且新版目录不再携带旧副本；失败与静默失败路径明确验证 Runtime 回到 v0.9.31 内嵌位置，持久目录不残留半迁移状态。
- Runtime 跨磁盘迁移需要复制回退时会记录本次持久副本；任一回滚路径都会删除该副本，避免旧版内嵌 Runtime 与残留持久 Runtime 并存。
- 保留已推送但未发布的 `v0.9.32` 标签，不移动或覆盖历史标签；使用新标签重新执行完整门禁。

### 验证

- 本地类型检查、115 项 Node 自动化测试、生产构建、NSIS 双安装包和更新清单一致性重新验证。
- 正式发布仍须通过 v0.9.31 → v0.9.33 的可见成功更新、可见失败回滚和静默失败退出码验收。

## v0.9.32 - 2026-08-30

### 修复与改进

- 远程更新安装从后台无反馈改为独立可见窗口，显示与真实安装阶段对应的百分比和中文状态。
- 更新进行中拦截关闭操作；新版健康检查、安装记录和快捷方式均完成后才显示 100% 并关闭窗口。
- 安装完成后不再由安装器自动重启 Stable；用户重新点击原快捷方式即可打开新版。
- 更新失败时窗口保持打开并显示回滚结论和错误码；Runtime 或旧目录恢复失败使用独立错误状态，不再误报“旧版本已恢复”。
- 兼容 v0.9.31 更新器遗留的 `/S --force-run --updated` 参数：新版安装器主动显示进度，同时忽略强制启动。
- 双份用户级/机器级安装并存时根据实际安装目录选择更新范围，避免更新错误副本。
- GitHub Release 流程增加标签/版本一致性保护和一次性 Windows runner 安装验收；可见成功更新、可见失败页、静默失败退出码和回滚均通过后才发布资产。

### 验证

- 114 项 Node 自动化测试、TypeScript 类型检查和 Vite 生产构建通过。
- 轻量更新包通过自定义 NSIS 编译，并生成对应 blockmap 与 `latest.yml`；更新清单的版本、文件名、大小和 SHA512 与安装包一致。
- 正式标签流水线会从已发布 v0.9.31 完整安装包建立基线，验证进度单调到 100%、不自动重启、快捷方式与新版健康检查、用户数据保留，以及强制失败后的旧版恢复和错误页保持打开。

## v0.9.31 - 2026-08-30

### 修复与改进

- 远程更新不再下载和重装完整 Harness Runtime；新增约 115 MB 的轻量更新包，完整安装包继续服务首次安装。
- 首次从旧版升级时把 Runtime 同盘迁移到持久目录，后续更新直接复用。
- 新版本先解压到安装目录旁的暂存目录，旧程序保持可用；准备完成后再通过目录重命名快速切换。
- 切换后执行应用与 Harness 健康检查；失败时关闭残留 Electron 子进程并恢复旧程序和旧 Runtime，恢复失败时不会误启动新版本。
- 恢复 electron-updater blockmap 差分下载，并保留完整轻量包回退。
- 卸载器支持自定义安装目录，通过 `_?=` 精确传递安装路径，并清理持久 Runtime。
- 版本号按固定规则从 `v0.9.30` 递增到 `v0.9.31`。

### 验证

- 110 项 Node 自动化测试、TypeScript 类型检查和 Vite 生产构建通过。
- 完整安装包与轻量更新包均成功生成；轻量包不包含 `resources/runtime`，`latest.yml` 正确指向轻量包。
- 隔离成功更新约 6.98 秒，安装与健康检查退出码均为 0；应用正常启动后才清理旧备份。
- 强制健康检查失败时约 8.05 秒完成回滚，安装器退出码 12，旧 EXE 与 Runtime 完整恢复。
- 完整首次安装退出码 0，Runtime Node、Harness 入口、卸载器和健康检查通过；QA 自定义目录卸载通过。
- 正式发布同时提供完整首次安装包、轻量更新包、轻量包 blockmap 和指向轻量包的 `latest.yml`；真实旧设备远程升级仍需发布后验收。

## v0.9.30 - 2026-08-28

### 修复与改进

- 用户主动选择的普通文件、ZIP 或文件夹会自动复制到工作区内的隔离附件目录，并把真实可操作路径交给 Agent；安装、解压和运行附件时不再要求用户手动搬到工作区。
- 附件授权仍限定为用户本次明确选择的副本，不扩大为任意本机路径访问。
- Windows 安装包从容易直接写入失败的 NSIS ZIP 解压链路切换为 7z 临时解包、复制重试链路，修复 `Failed to decompress files / Error opening output file(5)`。
- 恢复差分更新包和 blockmap 资产，远程更新继续支持完整安装包回退。

### 验证

- 108 项 Node 自动化测试通过；TypeScript 类型检查和 Vite 生产构建通过。
- v0.9.30 NSIS 安装包、blockmap 和 `latest.yml` 均已生成，安装包大小与 SHA-512 更新清单一致。
- 项目内隔离目录静默全新安装退出码为 0；安装后的应用版本、Runtime Node、Harness 入口和核心文件哈希均通过检查。
- 安装后应用离屏启动退出码为 0 并显示 `v0.9.30`；测试卸载退出码为 0，隔离安装目录已清理。
- GitHub Actions 正式发布通过；安装包、blockmap 和客户端 `latest.yml` 匿名直链均返回 HTTP 200。

## v0.9.29 - 2026-08-28（未单独发布，合并至 v0.9.30）

### 修复与改进

- 任务记录的编辑、删除操作区取消背景块、阴影、位移和过渡动画；操作按钮保持可用。
- 对话输入框在悬停、点击和聚焦时保持原样，不再出现内层背景块、阴影或过渡动画。
- 对话记录文字改为常规字重，并增加任务标题可用显示宽度。
- 普通文件、ZIP 压缩包和文件夹均可作为临时附件上传；Skill 文件夹继续使用独立安装入口。

### 验证

- 107 项 Node 自动化测试通过。
- TypeScript 类型检查通过。
- Vite 生产构建通过。
- 1443×904 隔离 Electron 离屏截图通过：任务操作区与聚焦输入框均保持无背景块、无阴影、无过渡动画，页面无横向溢出。

## v0.9.28 - 2026-08-28

### 修复与改进

- 用户确认“重启并更新”后，Windows 更新改为全程静默安装，不再打开需要手动操作的 NSIS 安装向导。
- 新安装器会识别旧版 updater 传入的 `--updated` 参数并自行切换为静默模式，确保 `v0.9.27 → v0.9.28` 本次升级也能跳过安装向导。
- 保留更新前的一次用户确认，下载完成不会突然关闭正在运行的 Stable。
- Windows 安装包改用普通压缩 ZIP 内部包，降低包含本地 Harness Runtime 时的解压开销；相比 7z/LZMA 会增加安装包体积和更新下载量，但避免无压缩包过度膨胀。
- 为确保 ZIP 快速安装模式真实生效，`v0.9.28` 不再生成 blockmap 差分包；首次安装仍可自定义安装目录。

## v0.9.27 - 2026-08-27

### 新增

- 定时自动化页：已配置、执行历史、任务模板，以及一次/每天/每周/每月计划。
- 支持在对话中让模型解析定时意图，并在确认卡片批准后创建任务。
- 基于 GitHub Releases 的应用内自动检查、后台下载和重启安装。
- 标签推送触发的 Windows GitHub Actions 发布流程。

### 修复与改进

- 用户消息写入数据库后立即同步到界面，复制内容连同附件和引用发送时不再等待模型结束才显示。
- 对话默认启用完整 Coding 能力：工作区读写、命令、依赖安装、联网工具与最多三层子 Agent；高风险操作继续人工确认。
- 对话成为第一导航项，“定时”替换“总览”。

### 验证

- TypeScript 类型检查通过。
- 101 项 Node 测试通过。
- Vite 生产构建通过。
- 1440×900 隔离 Electron 定时页截图通过，无横向溢出。

## v0.9.26 - 2026-08-25

### 新增

- Team 对话快照协作：从对话输入区选择在线设备并发送当前问答记录。
- 接收端确认与拒绝流程；接受后在对话页的 Team 专区显示快照。
- 单独的结果出口节点，可在工作流完成后打开文件或所在文件夹。

### 改进

- Agent 对话支持多任务切换、按需资源引用和执行过程卡片。
- 工作流支持模块化节点、并行分支执行及全链路连接动画。
- Skills 支持在对话中安装、在库中预览原始 `SKILL.md`。
- 权限审批、中文输入、脚本交互与输出文件生成流程得到完善。

### 变更

- Team 页面不再提供“派发远程任务”和“任务与审批”模块，保留设备与对话协作入口。
- 默认不发送整个工作区清单；仅在用户引用或 Agent 明确检索命中时加载数据、知识和 Skills。

### 验证

- TypeScript 类型检查。
- Node 测试套件。
- Vite 生产构建。
- Windows NSIS x64 安装包与 SHA-256 校验。
